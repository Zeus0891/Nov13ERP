// ======================================================================
// AUTOGENERATED FILE - DO NOT EDIT DIRECTLY
// Generated by scripts/generate-tables.ts
// Standard: ERP Multitenant (Nov-2025) BT/BH/BG base fields
// ======================================================================

// Source: Tables.v2.md
// Generated: 2025-11-15T07:03:05.959Z


// ======================================================
// BH — Base Hybrid (tenant + global)
// Count: 20
// ======================================================


model CRMEmail {
    // 🆔 Identity, Tenant and Global
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid
  globalId String @db.Uuid   // referencia a entidad BG

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("CONFIDENTIAL") @db.VarChar(50)

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCRMEmail", fields: [tenantId], references: [id], onDelete: Cascade)

    // Relation to the Global entity (adjust the real BG model)
  // global <YourGlobalModel> @relation(fields: [globalId], references: [id], onDelete: Restrict)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CRMEmailCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CRMEmailUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CRMEmailDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints
  @@unique([tenantId, globalId])   // un override por globalId en cada tenant
  @@index([globalId])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("crmemail")
}

model CRMMessageThread {
    // 🆔 Identity, Tenant and Global
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid
  globalId String @db.Uuid   // referencia a entidad BG

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("CONFIDENTIAL") @db.VarChar(50)

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCRMMessageThread", fields: [tenantId], references: [id], onDelete: Cascade)

    // Relation to the Global entity (adjust the real BG model)
  // global <YourGlobalModel> @relation(fields: [globalId], references: [id], onDelete: Restrict)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CRMMessageThreadCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CRMMessageThreadUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CRMMessageThreadDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints
  @@unique([tenantId, globalId])   // un override por globalId en cada tenant
  @@index([globalId])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("crmmessage_thread")
}

model ChangeOrder {
    // 🆔 Identity, Tenant and Global
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid
  globalId String @db.Uuid   // referencia a entidad BG

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("CONFIDENTIAL") @db.VarChar(50)

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToChangeOrder", fields: [tenantId], references: [id], onDelete: Cascade)

    // Relation to the Global entity (adjust the real BG model)
  // global <YourGlobalModel> @relation(fields: [globalId], references: [id], onDelete: Restrict)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ChangeOrderCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ChangeOrderUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ChangeOrderDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints
  @@unique([tenantId, globalId])   // un override por globalId en cada tenant
  @@index([globalId])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("change_order")
}

model CustomerPortalSession {
    // 🆔 Identity, Tenant and Global
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid
  globalId String @db.Uuid   // referencia a entidad BG

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("CONFIDENTIAL") @db.VarChar(50)

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCustomerPortalSession", fields: [tenantId], references: [id], onDelete: Cascade)

    // Relation to the Global entity (adjust the real BG model)
  // global <YourGlobalModel> @relation(fields: [globalId], references: [id], onDelete: Restrict)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CustomerPortalSessionCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CustomerPortalSessionUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CustomerPortalSessionDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints
  @@unique([tenantId, globalId])   // un override por globalId en cada tenant
  @@index([globalId])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("customer_portal_session")
}

model CustomerPortalUser {
    // 🆔 Identity, Tenant and Global
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid
  globalId String @db.Uuid   // referencia a entidad BG

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("CONFIDENTIAL") @db.VarChar(50)

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCustomerPortalUser", fields: [tenantId], references: [id], onDelete: Cascade)

    // Relation to the Global entity (adjust the real BG model)
  // global <YourGlobalModel> @relation(fields: [globalId], references: [id], onDelete: Restrict)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CustomerPortalUserCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CustomerPortalUserUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CustomerPortalUserDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints
  @@unique([tenantId, globalId])   // un override por globalId en cada tenant
  @@index([globalId])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("customer_portal_user")
}

model Dashboard {
    // 🆔 Identity, Tenant and Global
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid
  globalId String @db.Uuid   // referencia a entidad BG

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("CONFIDENTIAL") @db.VarChar(50)

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToDashboard", fields: [tenantId], references: [id], onDelete: Cascade)

    // Relation to the Global entity (adjust the real BG model)
  // global <YourGlobalModel> @relation(fields: [globalId], references: [id], onDelete: Restrict)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("DashboardCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("DashboardUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("DashboardDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints
  @@unique([tenantId, globalId])   // un override por globalId en cada tenant
  @@index([globalId])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("dashboard")
}

model Document {
    // 🆔 Identity, Tenant and Global
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid
  globalId String @db.Uuid   // referencia a entidad BG

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("CONFIDENTIAL") @db.VarChar(50)

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToDocument", fields: [tenantId], references: [id], onDelete: Cascade)

    // Relation to the Global entity (adjust the real BG model)
  // global <YourGlobalModel> @relation(fields: [globalId], references: [id], onDelete: Restrict)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("DocumentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("DocumentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("DocumentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints
  @@unique([tenantId, globalId])   // un override por globalId en cada tenant
  @@index([globalId])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("document")
}

model ESignatureEnvelope {
    // 🆔 Identity, Tenant and Global
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid
  globalId String @db.Uuid   // referencia a entidad BG

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("CONFIDENTIAL") @db.VarChar(50)

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToESignatureEnvelope", fields: [tenantId], references: [id], onDelete: Cascade)

    // Relation to the Global entity (adjust the real BG model)
  // global <YourGlobalModel> @relation(fields: [globalId], references: [id], onDelete: Restrict)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ESignatureEnvelopeCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ESignatureEnvelopeUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ESignatureEnvelopeDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints
  @@unique([tenantId, globalId])   // un override por globalId en cada tenant
  @@index([globalId])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("esignature_envelope")
}

model EmailCampaign {
    // 🆔 Identity, Tenant and Global
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid
  globalId String @db.Uuid   // referencia a entidad BG

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("CONFIDENTIAL") @db.VarChar(50)

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToEmailCampaign", fields: [tenantId], references: [id], onDelete: Cascade)

    // Relation to the Global entity (adjust the real BG model)
  // global <YourGlobalModel> @relation(fields: [globalId], references: [id], onDelete: Restrict)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("EmailCampaignCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("EmailCampaignUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("EmailCampaignDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints
  @@unique([tenantId, globalId])   // un override por globalId en cada tenant
  @@index([globalId])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("email_campaign")
}

model EmailMessage {
    // 🆔 Identity, Tenant and Global
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid
  globalId String @db.Uuid   // referencia a entidad BG

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("CONFIDENTIAL") @db.VarChar(50)

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToEmailMessage", fields: [tenantId], references: [id], onDelete: Cascade)

    // Relation to the Global entity (adjust the real BG model)
  // global <YourGlobalModel> @relation(fields: [globalId], references: [id], onDelete: Restrict)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("EmailMessageCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("EmailMessageUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("EmailMessageDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints
  @@unique([tenantId, globalId])   // un override por globalId en cada tenant
  @@index([globalId])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("email_message")
}

model Estimate {
    // 🆔 Identity, Tenant and Global
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid
  globalId String @db.Uuid   // referencia a entidad BG

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("CONFIDENTIAL") @db.VarChar(50)

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToEstimate", fields: [tenantId], references: [id], onDelete: Cascade)

    // Relation to the Global entity (adjust the real BG model)
  // global <YourGlobalModel> @relation(fields: [globalId], references: [id], onDelete: Restrict)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("EstimateCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("EstimateUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("EstimateDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints
  @@unique([tenantId, globalId])   // un override por globalId en cada tenant
  @@index([globalId])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("estimate")
}

model IntegrationWebhookDelivery {
    // 🆔 Identity, Tenant and Global
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid
  globalId String @db.Uuid   // referencia a entidad BG

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("CONFIDENTIAL") @db.VarChar(50)

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToIntegrationWebhookDelivery", fields: [tenantId], references: [id], onDelete: Cascade)

    // Relation to the Global entity (adjust the real BG model)
  // global <YourGlobalModel> @relation(fields: [globalId], references: [id], onDelete: Restrict)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("IntegrationWebhookDeliveryCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("IntegrationWebhookDeliveryUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("IntegrationWebhookDeliveryDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints
  @@unique([tenantId, globalId])   // un override por globalId en cada tenant
  @@index([globalId])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("integration_webhook_delivery")
}

model Invoice {
    // 🆔 Identity, Tenant and Global
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid
  globalId String @db.Uuid   // referencia a entidad BG

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("CONFIDENTIAL") @db.VarChar(50)

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToInvoice", fields: [tenantId], references: [id], onDelete: Cascade)

    // Relation to the Global entity (adjust the real BG model)
  // global <YourGlobalModel> @relation(fields: [globalId], references: [id], onDelete: Restrict)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("InvoiceCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("InvoiceUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("InvoiceDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints
  @@unique([tenantId, globalId])   // un override por globalId en cada tenant
  @@index([globalId])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("invoice")
}

model MessageThread {
    // 🆔 Identity, Tenant and Global
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid
  globalId String @db.Uuid   // referencia a entidad BG

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("CONFIDENTIAL") @db.VarChar(50)

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToMessageThread", fields: [tenantId], references: [id], onDelete: Cascade)

    // Relation to the Global entity (adjust the real BG model)
  // global <YourGlobalModel> @relation(fields: [globalId], references: [id], onDelete: Restrict)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("MessageThreadCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("MessageThreadUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("MessageThreadDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints
  @@unique([tenantId, globalId])   // un override por globalId en cada tenant
  @@index([globalId])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("message_thread")
}

model Notification {
    // 🆔 Identity, Tenant and Global
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid
  globalId String @db.Uuid   // referencia a entidad BG

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("CONFIDENTIAL") @db.VarChar(50)

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToNotification", fields: [tenantId], references: [id], onDelete: Cascade)

    // Relation to the Global entity (adjust the real BG model)
  // global <YourGlobalModel> @relation(fields: [globalId], references: [id], onDelete: Restrict)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("NotificationCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("NotificationUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("NotificationDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints
  @@unique([tenantId, globalId])   // un override por globalId en cada tenant
  @@index([globalId])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("notification")
}

model PhoneCall {
    // 🆔 Identity, Tenant and Global
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid
  globalId String @db.Uuid   // referencia a entidad BG

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("CONFIDENTIAL") @db.VarChar(50)

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToPhoneCall", fields: [tenantId], references: [id], onDelete: Cascade)

    // Relation to the Global entity (adjust the real BG model)
  // global <YourGlobalModel> @relation(fields: [globalId], references: [id], onDelete: Restrict)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("PhoneCallCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("PhoneCallUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("PhoneCallDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints
  @@unique([tenantId, globalId])   // un override por globalId en cada tenant
  @@index([globalId])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("phone_call")
}

model Project {
    // 🆔 Identity, Tenant and Global
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid
  globalId String @db.Uuid   // referencia a entidad BG

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("CONFIDENTIAL") @db.VarChar(50)

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToProject", fields: [tenantId], references: [id], onDelete: Cascade)

    // Relation to the Global entity (adjust the real BG model)
  // global <YourGlobalModel> @relation(fields: [globalId], references: [id], onDelete: Restrict)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ProjectCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ProjectUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ProjectDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints
  @@unique([tenantId, globalId])   // un override por globalId en cada tenant
  @@index([globalId])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("project")
}

model RFI {
    // 🆔 Identity, Tenant and Global
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid
  globalId String @db.Uuid   // referencia a entidad BG

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("CONFIDENTIAL") @db.VarChar(50)

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToRFI", fields: [tenantId], references: [id], onDelete: Cascade)

    // Relation to the Global entity (adjust the real BG model)
  // global <YourGlobalModel> @relation(fields: [globalId], references: [id], onDelete: Restrict)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("RFICreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("RFIUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("RFIDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints
  @@unique([tenantId, globalId])   // un override por globalId en cada tenant
  @@index([globalId])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("rfi")
}

model SMSMessage {
    // 🆔 Identity, Tenant and Global
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid
  globalId String @db.Uuid   // referencia a entidad BG

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("CONFIDENTIAL") @db.VarChar(50)

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToSMSMessage", fields: [tenantId], references: [id], onDelete: Cascade)

    // Relation to the Global entity (adjust the real BG model)
  // global <YourGlobalModel> @relation(fields: [globalId], references: [id], onDelete: Restrict)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("SMSMessageCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("SMSMessageUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("SMSMessageDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints
  @@unique([tenantId, globalId])   // un override por globalId en cada tenant
  @@index([globalId])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("smsmessage")
}

model Submittal {
    // 🆔 Identity, Tenant and Global
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid
  globalId String @db.Uuid   // referencia a entidad BG

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("CONFIDENTIAL") @db.VarChar(50)

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToSubmittal", fields: [tenantId], references: [id], onDelete: Cascade)

    // Relation to the Global entity (adjust the real BG model)
  // global <YourGlobalModel> @relation(fields: [globalId], references: [id], onDelete: Restrict)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("SubmittalCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("SubmittalUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("SubmittalDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints
  @@unique([tenantId, globalId])   // un override por globalId en cada tenant
  @@index([globalId])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("submittal")
}

// ======================================================
// BG — Base Global (shared masters)
// Count: 33
// ======================================================


model AIModel {
    // 🆔 Identity
  id String @id @default(uuid(7)) @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only if it's a critical global entity)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("PUBLIC") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)

    // 🗂️ Indexes & constraints (no tenant)
  @@index([status])
  @@index([deletedAt])
  @@index([createdAt], type: Brin)
  @@index([auditCorrelationId])
  @@index([dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([hashToken])
  // @@index([recordSource])
  // @@index([metadata], type: Gin)

  @@map("aimodel")
}

model AIModelVersion {
    // 🆔 Identity
  id String @id @default(uuid(7)) @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only if it's a critical global entity)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("PUBLIC") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)

    // 🗂️ Indexes & constraints (no tenant)
  @@index([status])
  @@index([deletedAt])
  @@index([createdAt], type: Brin)
  @@index([auditCorrelationId])
  @@index([dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([hashToken])
  // @@index([recordSource])
  // @@index([metadata], type: Gin)

  @@map("aimodel_version")
}

model AIPromptTemplate {
    // 🆔 Identity
  id String @id @default(uuid(7)) @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only if it's a critical global entity)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("PUBLIC") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)

    // 🗂️ Indexes & constraints (no tenant)
  @@index([status])
  @@index([deletedAt])
  @@index([createdAt], type: Brin)
  @@index([auditCorrelationId])
  @@index([dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([hashToken])
  // @@index([recordSource])
  // @@index([metadata], type: Gin)

  @@map("aiprompt_template")
}

model AccessResource {
    // 🆔 Identity
  id String @id @default(uuid(7)) @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only if it's a critical global entity)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("PUBLIC") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)

    // 🗂️ Indexes & constraints (no tenant)
  @@index([status])
  @@index([deletedAt])
  @@index([createdAt], type: Brin)
  @@index([auditCorrelationId])
  @@index([dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([hashToken])
  // @@index([recordSource])
  // @@index([metadata], type: Gin)

  @@map("access_resource")
}

model AccountLockout {
    // 🆔 Identity
  id String @id @default(uuid(7)) @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only if it's a critical global entity)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("PUBLIC") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)

    // 🗂️ Indexes & constraints (no tenant)
  @@index([status])
  @@index([deletedAt])
  @@index([createdAt], type: Brin)
  @@index([auditCorrelationId])
  @@index([dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([hashToken])
  // @@index([recordSource])
  // @@index([metadata], type: Gin)

  @@map("account_lockout")
}

model Actor {
    // 🆔 Identity
  id String @id @default(uuid(7)) @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only if it's a critical global entity)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("PUBLIC") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)

    // 🗂️ Indexes & constraints (no tenant)
  @@index([status])
  @@index([deletedAt])
  @@index([createdAt], type: Brin)
  @@index([auditCorrelationId])
  @@index([dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([hashToken])
  // @@index([recordSource])
  // @@index([metadata], type: Gin)

  @@map("actor")
}

model AuthFactor {
    // 🆔 Identity
  id String @id @default(uuid(7)) @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only if it's a critical global entity)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("PUBLIC") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)

    // 🗂️ Indexes & constraints (no tenant)
  @@index([status])
  @@index([deletedAt])
  @@index([createdAt], type: Brin)
  @@index([auditCorrelationId])
  @@index([dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([hashToken])
  // @@index([recordSource])
  // @@index([metadata], type: Gin)

  @@map("auth_factor")
}

model AuthFactorChallenge {
    // 🆔 Identity
  id String @id @default(uuid(7)) @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only if it's a critical global entity)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("PUBLIC") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)

    // 🗂️ Indexes & constraints (no tenant)
  @@index([status])
  @@index([deletedAt])
  @@index([createdAt], type: Brin)
  @@index([auditCorrelationId])
  @@index([dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([hashToken])
  // @@index([recordSource])
  // @@index([metadata], type: Gin)

  @@map("auth_factor_challenge")
}

model IdentityProvider {
    // 🆔 Identity
  id String @id @default(uuid(7)) @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only if it's a critical global entity)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("PUBLIC") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)

    // 🗂️ Indexes & constraints (no tenant)
  @@index([status])
  @@index([deletedAt])
  @@index([createdAt], type: Brin)
  @@index([auditCorrelationId])
  @@index([dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([hashToken])
  // @@index([recordSource])
  // @@index([metadata], type: Gin)

  @@map("identity_provider")
}

model PasswordResetToken {
    // 🆔 Identity
  id String @id @default(uuid(7)) @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only if it's a critical global entity)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("PUBLIC") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)

    // 🗂️ Indexes & constraints (no tenant)
  @@index([status])
  @@index([deletedAt])
  @@index([createdAt], type: Brin)
  @@index([auditCorrelationId])
  @@index([dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([hashToken])
  // @@index([recordSource])
  // @@index([metadata], type: Gin)

  @@map("password_reset_token")
}

model Permission {
    // 🆔 Identity
  id String @id @default(uuid(7)) @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only if it's a critical global entity)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("PUBLIC") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)

    // 🗂️ Indexes & constraints (no tenant)
  @@index([status])
  @@index([deletedAt])
  @@index([createdAt], type: Brin)
  @@index([auditCorrelationId])
  @@index([dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([hashToken])
  // @@index([recordSource])
  // @@index([metadata], type: Gin)

  @@map("permission")
}

model RecoveryCode {
    // 🆔 Identity
  id String @id @default(uuid(7)) @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only if it's a critical global entity)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("PUBLIC") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)

    // 🗂️ Indexes & constraints (no tenant)
  @@index([status])
  @@index([deletedAt])
  @@index([createdAt], type: Brin)
  @@index([auditCorrelationId])
  @@index([dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([hashToken])
  // @@index([recordSource])
  // @@index([metadata], type: Gin)

  @@map("recovery_code")
}

model SSOSession {
    // 🆔 Identity
  id String @id @default(uuid(7)) @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only if it's a critical global entity)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("PUBLIC") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)

    // 🗂️ Indexes & constraints (no tenant)
  @@index([status])
  @@index([deletedAt])
  @@index([createdAt], type: Brin)
  @@index([auditCorrelationId])
  @@index([dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([hashToken])
  // @@index([recordSource])
  // @@index([metadata], type: Gin)

  @@map("ssosession")
}

model SecurityEvent {
    // 🆔 Identity
  id String @id @default(uuid(7)) @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only if it's a critical global entity)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("PUBLIC") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)

    // 🗂️ Indexes & constraints (no tenant)
  @@index([status])
  @@index([deletedAt])
  @@index([createdAt], type: Brin)
  @@index([auditCorrelationId])
  @@index([dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([hashToken])
  // @@index([recordSource])
  // @@index([metadata], type: Gin)

  @@map("security_event")
}

model Session {
    // 🆔 Identity
  id String @id @default(uuid(7)) @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only if it's a critical global entity)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("PUBLIC") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)

    // 🗂️ Indexes & constraints (no tenant)
  @@index([status])
  @@index([deletedAt])
  @@index([createdAt], type: Brin)
  @@index([auditCorrelationId])
  @@index([dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([hashToken])
  // @@index([recordSource])
  // @@index([metadata], type: Gin)

  @@map("session")
}

model Tenant {
    // 🆔 Identity
  id String @id @default(uuid(7)) @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only if it's a critical global entity)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("PUBLIC") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)

    // 🗂️ Indexes & constraints (no tenant)
  @@index([status])
  @@index([deletedAt])
  @@index([createdAt], type: Brin)
  @@index([auditCorrelationId])
  @@index([dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([hashToken])
  // @@index([recordSource])
  // @@index([metadata], type: Gin)

  @@map("tenant")
}

model TenantBranding {
    // 🆔 Identity
  id String @id @default(uuid(7)) @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only if it's a critical global entity)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("PUBLIC") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)

    // 🗂️ Indexes & constraints (no tenant)
  @@index([status])
  @@index([deletedAt])
  @@index([createdAt], type: Brin)
  @@index([auditCorrelationId])
  @@index([dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([hashToken])
  // @@index([recordSource])
  // @@index([metadata], type: Gin)

  @@map("tenant_branding")
}

model TenantComplianceSetting {
    // 🆔 Identity
  id String @id @default(uuid(7)) @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only if it's a critical global entity)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("PUBLIC") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)

    // 🗂️ Indexes & constraints (no tenant)
  @@index([status])
  @@index([deletedAt])
  @@index([createdAt], type: Brin)
  @@index([auditCorrelationId])
  @@index([dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([hashToken])
  // @@index([recordSource])
  // @@index([metadata], type: Gin)

  @@map("tenant_compliance_setting")
}

model TenantDomain {
    // 🆔 Identity
  id String @id @default(uuid(7)) @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only if it's a critical global entity)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("PUBLIC") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)

    // 🗂️ Indexes & constraints (no tenant)
  @@index([status])
  @@index([deletedAt])
  @@index([createdAt], type: Brin)
  @@index([auditCorrelationId])
  @@index([dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([hashToken])
  // @@index([recordSource])
  // @@index([metadata], type: Gin)

  @@map("tenant_domain")
}

model TenantFeatureFlag {
    // 🆔 Identity
  id String @id @default(uuid(7)) @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only if it's a critical global entity)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("PUBLIC") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)

    // 🗂️ Indexes & constraints (no tenant)
  @@index([status])
  @@index([deletedAt])
  @@index([createdAt], type: Brin)
  @@index([auditCorrelationId])
  @@index([dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([hashToken])
  // @@index([recordSource])
  // @@index([metadata], type: Gin)

  @@map("tenant_feature_flag")
}

model TenantHistoryEvent {
    // 🆔 Identity
  id String @id @default(uuid(7)) @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only if it's a critical global entity)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("PUBLIC") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)

    // 🗂️ Indexes & constraints (no tenant)
  @@index([status])
  @@index([deletedAt])
  @@index([createdAt], type: Brin)
  @@index([auditCorrelationId])
  @@index([dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([hashToken])
  // @@index([recordSource])
  // @@index([metadata], type: Gin)

  @@map("tenant_history_event")
}

model TenantModule {
    // 🆔 Identity
  id String @id @default(uuid(7)) @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only if it's a critical global entity)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("PUBLIC") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)

    // 🗂️ Indexes & constraints (no tenant)
  @@index([status])
  @@index([deletedAt])
  @@index([createdAt], type: Brin)
  @@index([auditCorrelationId])
  @@index([dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([hashToken])
  // @@index([recordSource])
  // @@index([metadata], type: Gin)

  @@map("tenant_module")
}

model TenantSettings {
    // 🆔 Identity
  id String @id @default(uuid(7)) @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only if it's a critical global entity)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("PUBLIC") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)

    // 🗂️ Indexes & constraints (no tenant)
  @@index([status])
  @@index([deletedAt])
  @@index([createdAt], type: Brin)
  @@index([auditCorrelationId])
  @@index([dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([hashToken])
  // @@index([recordSource])
  // @@index([metadata], type: Gin)

  @@map("tenant_settings")
}

model TenantSubscription {
    // 🆔 Identity
  id String @id @default(uuid(7)) @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only if it's a critical global entity)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("PUBLIC") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)

    // 🗂️ Indexes & constraints (no tenant)
  @@index([status])
  @@index([deletedAt])
  @@index([createdAt], type: Brin)
  @@index([auditCorrelationId])
  @@index([dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([hashToken])
  // @@index([recordSource])
  // @@index([metadata], type: Gin)

  @@map("tenant_subscription")
}

model TenantUsageRecord {
    // 🆔 Identity
  id String @id @default(uuid(7)) @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only if it's a critical global entity)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("PUBLIC") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)

    // 🗂️ Indexes & constraints (no tenant)
  @@index([status])
  @@index([deletedAt])
  @@index([createdAt], type: Brin)
  @@index([auditCorrelationId])
  @@index([dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([hashToken])
  // @@index([recordSource])
  // @@index([metadata], type: Gin)

  @@map("tenant_usage_record")
}

model User {
    // 🆔 Identity
  id String @id @default(uuid(7)) @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only if it's a critical global entity)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("PUBLIC") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)

    // 🗂️ Indexes & constraints (no tenant)
  @@index([status])
  @@index([deletedAt])
  @@index([createdAt], type: Brin)
  @@index([auditCorrelationId])
  @@index([dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([hashToken])
  // @@index([recordSource])
  // @@index([metadata], type: Gin)

  @@map("user")
}

model UserApiKey {
    // 🆔 Identity
  id String @id @default(uuid(7)) @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only if it's a critical global entity)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("PUBLIC") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)

    // 🗂️ Indexes & constraints (no tenant)
  @@index([status])
  @@index([deletedAt])
  @@index([createdAt], type: Brin)
  @@index([auditCorrelationId])
  @@index([dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([hashToken])
  // @@index([recordSource])
  // @@index([metadata], type: Gin)

  @@map("user_api_key")
}

model UserDevice {
    // 🆔 Identity
  id String @id @default(uuid(7)) @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only if it's a critical global entity)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("PUBLIC") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)

    // 🗂️ Indexes & constraints (no tenant)
  @@index([status])
  @@index([deletedAt])
  @@index([createdAt], type: Brin)
  @@index([auditCorrelationId])
  @@index([dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([hashToken])
  // @@index([recordSource])
  // @@index([metadata], type: Gin)

  @@map("user_device")
}

model UserDeviceHistory {
    // 🆔 Identity
  id String @id @default(uuid(7)) @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only if it's a critical global entity)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("PUBLIC") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)

    // 🗂️ Indexes & constraints (no tenant)
  @@index([status])
  @@index([deletedAt])
  @@index([createdAt], type: Brin)
  @@index([auditCorrelationId])
  @@index([dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([hashToken])
  // @@index([recordSource])
  // @@index([metadata], type: Gin)

  @@map("user_device_history")
}

model UserHistoryEvent {
    // 🆔 Identity
  id String @id @default(uuid(7)) @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only if it's a critical global entity)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("PUBLIC") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)

    // 🗂️ Indexes & constraints (no tenant)
  @@index([status])
  @@index([deletedAt])
  @@index([createdAt], type: Brin)
  @@index([auditCorrelationId])
  @@index([dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([hashToken])
  // @@index([recordSource])
  // @@index([metadata], type: Gin)

  @@map("user_history_event")
}

model UserInvitation {
    // 🆔 Identity
  id String @id @default(uuid(7)) @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only if it's a critical global entity)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("PUBLIC") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)

    // 🗂️ Indexes & constraints (no tenant)
  @@index([status])
  @@index([deletedAt])
  @@index([createdAt], type: Brin)
  @@index([auditCorrelationId])
  @@index([dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([hashToken])
  // @@index([recordSource])
  // @@index([metadata], type: Gin)

  @@map("user_invitation")
}

model UserProfile {
    // 🆔 Identity
  id String @id @default(uuid(7)) @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only if it's a critical global entity)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("PUBLIC") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)

    // 🗂️ Indexes & constraints (no tenant)
  @@index([status])
  @@index([deletedAt])
  @@index([createdAt], type: Brin)
  @@index([auditCorrelationId])
  @@index([dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([hashToken])
  // @@index([recordSource])
  // @@index([metadata], type: Gin)

  @@map("user_profile")
}

model UserSetting {
    // 🆔 Identity
  id String @id @default(uuid(7)) @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only if it's a critical global entity)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("PUBLIC") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?

    // 🔎 Observability (OPTIONAL)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
  // hashToken    String? @db.VarChar(255)
  // metadata     Json?   @db.JsonB
  // recordSource String? @db.VarChar(50)

    // 🗂️ Indexes & constraints (no tenant)
  @@index([status])
  @@index([deletedAt])
  @@index([createdAt], type: Brin)
  @@index([auditCorrelationId])
  @@index([dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([hashToken])
  // @@index([recordSource])
  // @@index([metadata], type: Gin)

  @@map("user_setting")
}

// ======================================================
// BT — Base Tenant (tenant-scoped)
// Count: 569
// ======================================================


model AIAction {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToAIAction", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("AIActionCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("AIActionUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("AIActionDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("aiaction")
}

model AIActionRun {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToAIActionRun", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("AIActionRunCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("AIActionRunUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("AIActionRunDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("aiaction_run")
}

model AIAnnotation {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToAIAnnotation", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("AIAnnotationCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("AIAnnotationUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("AIAnnotationDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("aiannotation")
}

model AIAnomaly {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToAIAnomaly", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("AIAnomalyCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("AIAnomalyUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("AIAnomalyDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("aianomaly")
}

model AIClassificationResult {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToAIClassificationResult", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("AIClassificationResultCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("AIClassificationResultUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("AIClassificationResultDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("aiclassification_result")
}

model AIDocumentAttachment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToAIDocumentAttachment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("AIDocumentAttachmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("AIDocumentAttachmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("AIDocumentAttachmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("aidocument_attachment")
}

model AIDocumentChunk {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToAIDocumentChunk", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("AIDocumentChunkCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("AIDocumentChunkUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("AIDocumentChunkDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("aidocument_chunk")
}

model AIDocumentHistoryEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToAIDocumentHistoryEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("AIDocumentHistoryEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("AIDocumentHistoryEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("AIDocumentHistoryEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("aidocument_history_event")
}

model AIDocumentIndex {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToAIDocumentIndex", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("AIDocumentIndexCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("AIDocumentIndexUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("AIDocumentIndexDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("aidocument_index")
}

model AIEmbedding {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToAIEmbedding", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("AIEmbeddingCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("AIEmbeddingUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("AIEmbeddingDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("aiembedding")
}

model AIEntity {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToAIEntity", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("AIEntityCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("AIEntityUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("AIEntityDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("aientity")
}

model AIExtractionResult {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToAIExtractionResult", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("AIExtractionResultCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("AIExtractionResultUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("AIExtractionResultDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("aiextraction_result")
}

model AIForecast {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToAIForecast", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("AIForecastCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("AIForecastUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("AIForecastDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("aiforecast")
}

model AIInsight {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToAIInsight", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("AIInsightCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("AIInsightUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("AIInsightDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("aiinsight")
}

model AIInsightAttachment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToAIInsightAttachment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("AIInsightAttachmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("AIInsightAttachmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("AIInsightAttachmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("aiinsight_attachment")
}

model AIInsightFeedback {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToAIInsightFeedback", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("AIInsightFeedbackCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("AIInsightFeedbackUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("AIInsightFeedbackDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("aiinsight_feedback")
}

model AIInsightHistory {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToAIInsightHistory", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("AIInsightHistoryCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("AIInsightHistoryUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("AIInsightHistoryDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("aiinsight_history")
}

model AIJob {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToAIJob", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("AIJobCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("AIJobUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("AIJobDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("aijob")
}

model AIJobArtifact {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToAIJobArtifact", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("AIJobArtifactCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("AIJobArtifactUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("AIJobArtifactDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("aijob_artifact")
}

model AIOCRResult {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToAIOCRResult", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("AIOCRResultCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("AIOCRResultUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("AIOCRResultDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("aiocrresult")
}

model AIPlaybook {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToAIPlaybook", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("AIPlaybookCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("AIPlaybookUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("AIPlaybookDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("aiplaybook")
}

model AIPlaybookStep {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToAIPlaybookStep", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("AIPlaybookStepCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("AIPlaybookStepUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("AIPlaybookStepDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("aiplaybook_step")
}

model AIPrediction {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToAIPrediction", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("AIPredictionCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("AIPredictionUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("AIPredictionDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("aiprediction")
}

model AIRecommendation {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToAIRecommendation", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("AIRecommendationCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("AIRecommendationUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("AIRecommendationDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("airecommendation")
}

model AITrend {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToAITrend", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("AITrendCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("AITrendUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("AITrendDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("aitrend")
}

model AIWhatIfRun {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToAIWhatIfRun", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("AIWhatIfRunCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("AIWhatIfRunUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("AIWhatIfRunDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("aiwhat_if_run")
}

model AccessAuditEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToAccessAuditEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("AccessAuditEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("AccessAuditEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("AccessAuditEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("access_audit_event")
}

model AccessPolicy {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToAccessPolicy", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("AccessPolicyCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("AccessPolicyUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("AccessPolicyDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("access_policy")
}

model AccessPolicyCondition {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToAccessPolicyCondition", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("AccessPolicyConditionCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("AccessPolicyConditionUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("AccessPolicyConditionDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("access_policy_condition")
}

model AccessScope {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToAccessScope", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("AccessScopeCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("AccessScopeUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("AccessScopeDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("access_scope")
}

model AccessScopeAssignment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToAccessScopeAssignment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("AccessScopeAssignmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("AccessScopeAssignmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("AccessScopeAssignmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("access_scope_assignment")
}

model AnalyticsAttachment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToAnalyticsAttachment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("AnalyticsAttachmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("AnalyticsAttachmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("AnalyticsAttachmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("analytics_attachment")
}

model AnalyticsCube {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToAnalyticsCube", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("AnalyticsCubeCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("AnalyticsCubeUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("AnalyticsCubeDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("analytics_cube")
}

model AnalyticsDataset {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToAnalyticsDataset", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("AnalyticsDatasetCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("AnalyticsDatasetUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("AnalyticsDatasetDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("analytics_dataset")
}

model AnalyticsDatasetField {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToAnalyticsDatasetField", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("AnalyticsDatasetFieldCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("AnalyticsDatasetFieldUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("AnalyticsDatasetFieldDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("analytics_dataset_field")
}

model AnalyticsDimension {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToAnalyticsDimension", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("AnalyticsDimensionCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("AnalyticsDimensionUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("AnalyticsDimensionDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("analytics_dimension")
}

model AnalyticsFilter {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToAnalyticsFilter", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("AnalyticsFilterCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("AnalyticsFilterUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("AnalyticsFilterDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("analytics_filter")
}

model AnalyticsInsight {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToAnalyticsInsight", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("AnalyticsInsightCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("AnalyticsInsightUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("AnalyticsInsightDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("analytics_insight")
}

model AnalyticsMetric {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToAnalyticsMetric", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("AnalyticsMetricCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("AnalyticsMetricUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("AnalyticsMetricDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("analytics_metric")
}

model AnalyticsQuery {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToAnalyticsQuery", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("AnalyticsQueryCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("AnalyticsQueryUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("AnalyticsQueryDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("analytics_query")
}

model ApprovalAssignment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToApprovalAssignment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ApprovalAssignmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ApprovalAssignmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ApprovalAssignmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("approval_assignment")
}

model ApprovalAttachment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToApprovalAttachment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ApprovalAttachmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ApprovalAttachmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ApprovalAttachmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("approval_attachment")
}

model ApprovalCondition {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToApprovalCondition", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ApprovalConditionCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ApprovalConditionUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ApprovalConditionDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("approval_condition")
}

model ApprovalDecision {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToApprovalDecision", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ApprovalDecisionCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ApprovalDecisionUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ApprovalDecisionDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("approval_decision")
}

model ApprovalEscalation {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToApprovalEscalation", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ApprovalEscalationCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ApprovalEscalationUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ApprovalEscalationDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("approval_escalation")
}

model ApprovalHistoryEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToApprovalHistoryEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ApprovalHistoryEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ApprovalHistoryEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ApprovalHistoryEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("approval_history_event")
}

model ApprovalLevel {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToApprovalLevel", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ApprovalLevelCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ApprovalLevelUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ApprovalLevelDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("approval_level")
}

model ApprovalRequest {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToApprovalRequest", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ApprovalRequestCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ApprovalRequestUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ApprovalRequestDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("approval_request")
}

model ApprovalRule {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToApprovalRule", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ApprovalRuleCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ApprovalRuleUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ApprovalRuleDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("approval_rule")
}

model ApprovalStep {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToApprovalStep", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ApprovalStepCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ApprovalStepUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ApprovalStepDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("approval_step")
}

model BankAccount {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToBankAccount", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("BankAccountCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("BankAccountUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("BankAccountDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("bank_account")
}

model BankDeposit {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToBankDeposit", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("BankDepositCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("BankDepositUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("BankDepositDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("bank_deposit")
}

model BankFeedConnection {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToBankFeedConnection", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("BankFeedConnectionCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("BankFeedConnectionUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("BankFeedConnectionDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("bank_feed_connection")
}

model BankHistoryEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToBankHistoryEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("BankHistoryEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("BankHistoryEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("BankHistoryEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("bank_history_event")
}

model BankReconciliation {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToBankReconciliation", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("BankReconciliationCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("BankReconciliationUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("BankReconciliationDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("bank_reconciliation")
}

model BankReconciliationItem {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToBankReconciliationItem", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("BankReconciliationItemCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("BankReconciliationItemUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("BankReconciliationItemDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("bank_reconciliation_item")
}

model BankRule {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToBankRule", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("BankRuleCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("BankRuleUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("BankRuleDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("bank_rule")
}

model BankStatement {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToBankStatement", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("BankStatementCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("BankStatementUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("BankStatementDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("bank_statement")
}

model BankTransaction {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToBankTransaction", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("BankTransactionCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("BankTransactionUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("BankTransactionDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("bank_transaction")
}

model BankTransfer {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToBankTransfer", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("BankTransferCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("BankTransferUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("BankTransferDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("bank_transfer")
}

model BillingAdjustment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToBillingAdjustment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("BillingAdjustmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("BillingAdjustmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("BillingAdjustmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("billing_adjustment")
}

model BillingDeposit {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToBillingDeposit", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("BillingDepositCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("BillingDepositUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("BillingDepositDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("billing_deposit")
}

model BillingHistoryEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToBillingHistoryEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("BillingHistoryEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("BillingHistoryEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("BillingHistoryEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("billing_history_event")
}

model BillingMilestone {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToBillingMilestone", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("BillingMilestoneCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("BillingMilestoneUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("BillingMilestoneDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("billing_milestone")
}

model BillingProgress {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToBillingProgress", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("BillingProgressCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("BillingProgressUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("BillingProgressDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("billing_progress")
}

model BillingRetainage {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToBillingRetainage", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("BillingRetainageCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("BillingRetainageUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("BillingRetainageDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("billing_retainage")
}

model BillingSchedule {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToBillingSchedule", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("BillingScheduleCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("BillingScheduleUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("BillingScheduleDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("billing_schedule")
}

model CRMAccount {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCRMAccount", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CRMAccountCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CRMAccountUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CRMAccountDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("crmaccount")
}

model CRMAccountHierarchy {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCRMAccountHierarchy", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CRMAccountHierarchyCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CRMAccountHierarchyUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CRMAccountHierarchyDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("crmaccount_hierarchy")
}

model CRMAccountRelationship {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCRMAccountRelationship", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CRMAccountRelationshipCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CRMAccountRelationshipUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CRMAccountRelationshipDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("crmaccount_relationship")
}

model CRMAccountTag {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCRMAccountTag", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CRMAccountTagCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CRMAccountTagUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CRMAccountTagDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("crmaccount_tag")
}

model CRMActivity {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCRMActivity", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CRMActivityCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CRMActivityUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CRMActivityDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("crmactivity")
}

model CRMAddress {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCRMAddress", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CRMAddressCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CRMAddressUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CRMAddressDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("crmaddress")
}

model CRMChannel {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCRMChannel", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CRMChannelCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CRMChannelUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CRMChannelDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("crmchannel")
}

model CRMContact {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCRMContact", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CRMContactCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CRMContactUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CRMContactDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("crmcontact")
}

model CRMContactRole {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCRMContactRole", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CRMContactRoleCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CRMContactRoleUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CRMContactRoleDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("crmcontact_role")
}

model CRMDecisionMaker {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCRMDecisionMaker", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CRMDecisionMakerCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CRMDecisionMakerUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CRMDecisionMakerDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("crmdecision_maker")
}

model CRMEmailAttachment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCRMEmailAttachment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CRMEmailAttachmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CRMEmailAttachmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CRMEmailAttachmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("crmemail_attachment")
}

model CRMHistoryEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCRMHistoryEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CRMHistoryEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CRMHistoryEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CRMHistoryEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("crmhistory_event")
}

model CRMHousehold {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCRMHousehold", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CRMHouseholdCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CRMHouseholdUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CRMHouseholdDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("crmhousehold")
}

model CRMHouseholdMember {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCRMHouseholdMember", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CRMHouseholdMemberCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CRMHouseholdMemberUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CRMHouseholdMemberDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("crmhousehold_member")
}

model CRMInfluencer {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCRMInfluencer", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CRMInfluencerCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CRMInfluencerUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CRMInfluencerDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("crminfluencer")
}

model CRMInteraction {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCRMInteraction", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CRMInteractionCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CRMInteractionUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CRMInteractionDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("crminteraction")
}

model CRMInteractionAttachment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCRMInteractionAttachment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CRMInteractionAttachmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CRMInteractionAttachmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CRMInteractionAttachmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("crminteraction_attachment")
}

model CRMMessageParticipant {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCRMMessageParticipant", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CRMMessageParticipantCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CRMMessageParticipantUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CRMMessageParticipantDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("crmmessage_participant")
}

model CRMNote {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCRMNote", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CRMNoteCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CRMNoteUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CRMNoteDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("crmnote")
}

model CRMNotificationEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCRMNotificationEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CRMNotificationEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CRMNotificationEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CRMNotificationEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("crmnotification_event")
}

model CRMNotificationSetting {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCRMNotificationSetting", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CRMNotificationSettingCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CRMNotificationSettingUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CRMNotificationSettingDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("crmnotification_setting")
}

model CRMPartner {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCRMPartner", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CRMPartnerCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CRMPartnerUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CRMPartnerDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("crmpartner")
}

model CRMPhoneCall {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCRMPhoneCall", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CRMPhoneCallCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CRMPhoneCallUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CRMPhoneCallDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("crmphone_call")
}

model CRMPhoneCallRecording {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCRMPhoneCallRecording", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CRMPhoneCallRecordingCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CRMPhoneCallRecordingUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CRMPhoneCallRecordingDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("crmphone_call_recording")
}

model CRMRelationshipAttachment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCRMRelationshipAttachment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CRMRelationshipAttachmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CRMRelationshipAttachmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CRMRelationshipAttachmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("crmrelationship_attachment")
}

model CRMRelationshipHistoryEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCRMRelationshipHistoryEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CRMRelationshipHistoryEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CRMRelationshipHistoryEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CRMRelationshipHistoryEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("crmrelationship_history_event")
}

model CRMSMS {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCRMSMS", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CRMSMSCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CRMSMSUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CRMSMSDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("crmsms")
}

model CRMTag {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCRMTag", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CRMTagCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CRMTagUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CRMTagDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("crmtag")
}

model ChangeOrderAttachment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToChangeOrderAttachment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ChangeOrderAttachmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ChangeOrderAttachmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ChangeOrderAttachmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("change_order_attachment")
}

model ChangeOrderHistoryEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToChangeOrderHistoryEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ChangeOrderHistoryEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ChangeOrderHistoryEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ChangeOrderHistoryEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("change_order_history_event")
}

model ChangeOrderImpact {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToChangeOrderImpact", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ChangeOrderImpactCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ChangeOrderImpactUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ChangeOrderImpactDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("change_order_impact")
}

model ChangeOrderLineItem {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToChangeOrderLineItem", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ChangeOrderLineItemCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ChangeOrderLineItemUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ChangeOrderLineItemDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("change_order_line_item")
}

model ChangeOrderReason {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToChangeOrderReason", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ChangeOrderReasonCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ChangeOrderReasonUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ChangeOrderReasonDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("change_order_reason")
}

model ChangeOrderRevision {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToChangeOrderRevision", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ChangeOrderRevisionCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ChangeOrderRevisionUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ChangeOrderRevisionDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("change_order_revision")
}

model ChangeOrderScheduleImpact {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToChangeOrderScheduleImpact", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ChangeOrderScheduleImpactCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ChangeOrderScheduleImpactUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ChangeOrderScheduleImpactDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("change_order_schedule_impact")
}

model ChangeOrderScope {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToChangeOrderScope", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ChangeOrderScopeCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ChangeOrderScopeUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ChangeOrderScopeDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("change_order_scope")
}

model CommunicationProvider {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCommunicationProvider", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CommunicationProviderCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CommunicationProviderUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CommunicationProviderDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("communication_provider")
}

model ComplianceAttachment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToComplianceAttachment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ComplianceAttachmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ComplianceAttachmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ComplianceAttachmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("compliance_attachment")
}

model ComplianceAudit {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToComplianceAudit", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ComplianceAuditCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ComplianceAuditUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ComplianceAuditDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("compliance_audit")
}

model ComplianceAuditFinding {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToComplianceAuditFinding", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ComplianceAuditFindingCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ComplianceAuditFindingUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ComplianceAuditFindingDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("compliance_audit_finding")
}

model ComplianceCheck {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToComplianceCheck", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ComplianceCheckCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ComplianceCheckUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ComplianceCheckDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("compliance_check")
}

model ComplianceCorrectionAction {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToComplianceCorrectionAction", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ComplianceCorrectionActionCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ComplianceCorrectionActionUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ComplianceCorrectionActionDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("compliance_correction_action")
}

model ComplianceDocument {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToComplianceDocument", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ComplianceDocumentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ComplianceDocumentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ComplianceDocumentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("compliance_document")
}

model ComplianceHistory {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToComplianceHistory", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ComplianceHistoryCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ComplianceHistoryUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ComplianceHistoryDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("compliance_history")
}

model ComplianceRequirement {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToComplianceRequirement", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ComplianceRequirementCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ComplianceRequirementUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ComplianceRequirementDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("compliance_requirement")
}

model ComplianceTrainingRecord {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToComplianceTrainingRecord", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ComplianceTrainingRecordCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ComplianceTrainingRecordUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ComplianceTrainingRecordDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("compliance_training_record")
}

model ComplianceViolation {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToComplianceViolation", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ComplianceViolationCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ComplianceViolationUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ComplianceViolationDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("compliance_violation")
}

model Contract {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToContract", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ContractCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ContractUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ContractDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("contract")
}

model ContractAmendment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToContractAmendment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ContractAmendmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ContractAmendmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ContractAmendmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("contract_amendment")
}

model ContractAttachment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToContractAttachment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ContractAttachmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ContractAttachmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ContractAttachmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("contract_attachment")
}

model ContractCompliance {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToContractCompliance", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ContractComplianceCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ContractComplianceUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ContractComplianceDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("contract_compliance")
}

model ContractDeliverable {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToContractDeliverable", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ContractDeliverableCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ContractDeliverableUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ContractDeliverableDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("contract_deliverable")
}

model ContractHistoryEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToContractHistoryEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ContractHistoryEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ContractHistoryEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ContractHistoryEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("contract_history_event")
}

model ContractMilestone {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToContractMilestone", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ContractMilestoneCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ContractMilestoneUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ContractMilestoneDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("contract_milestone")
}

model ContractScope {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToContractScope", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ContractScopeCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ContractScopeUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ContractScopeDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("contract_scope")
}

model ContractSignature {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToContractSignature", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ContractSignatureCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ContractSignatureUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ContractSignatureDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("contract_signature")
}

model ContractTerm {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToContractTerm", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ContractTermCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ContractTermUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ContractTermDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("contract_term")
}

model CorpCard {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCorpCard", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CorpCardCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CorpCardUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CorpCardDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("corp_card")
}

model CorpCardAttachment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCorpCardAttachment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CorpCardAttachmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CorpCardAttachmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CorpCardAttachmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("corp_card_attachment")
}

model CorpCardDispute {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCorpCardDispute", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CorpCardDisputeCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CorpCardDisputeUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CorpCardDisputeDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("corp_card_dispute")
}

model CorpCardHistoryEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCorpCardHistoryEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CorpCardHistoryEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CorpCardHistoryEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CorpCardHistoryEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("corp_card_history_event")
}

model CorpCardLimit {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCorpCardLimit", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CorpCardLimitCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CorpCardLimitUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CorpCardLimitDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("corp_card_limit")
}

model CorpCardReceipt {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCorpCardReceipt", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CorpCardReceiptCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CorpCardReceiptUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CorpCardReceiptDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("corp_card_receipt")
}

model CorpCardReconciliation {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCorpCardReconciliation", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CorpCardReconciliationCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CorpCardReconciliationUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CorpCardReconciliationDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("corp_card_reconciliation")
}

model CorpCardTransaction {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCorpCardTransaction", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CorpCardTransactionCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CorpCardTransactionUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CorpCardTransactionDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("corp_card_transaction")
}

model CorpCardVendor {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCorpCardVendor", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CorpCardVendorCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CorpCardVendorUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CorpCardVendorDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("corp_card_vendor")
}

model CostCategory {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCostCategory", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CostCategoryCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CostCategoryUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CostCategoryDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("cost_category")
}

model CostCenter {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCostCenter", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CostCenterCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CostCenterUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CostCenterDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("cost_center")
}

model CostCode {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCostCode", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CostCodeCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CostCodeUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CostCodeDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("cost_code")
}

model CostType {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCostType", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CostTypeCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CostTypeUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CostTypeDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("cost_type")
}

model CustomerPortalAccess {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCustomerPortalAccess", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CustomerPortalAccessCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CustomerPortalAccessUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CustomerPortalAccessDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("customer_portal_access")
}

model CustomerPortalDocument {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCustomerPortalDocument", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CustomerPortalDocumentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CustomerPortalDocumentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CustomerPortalDocumentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("customer_portal_document")
}

model CustomerPortalEstimateView {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCustomerPortalEstimateView", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CustomerPortalEstimateViewCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CustomerPortalEstimateViewUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CustomerPortalEstimateViewDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("customer_portal_estimate_view")
}

model CustomerPortalHistoryEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCustomerPortalHistoryEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CustomerPortalHistoryEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CustomerPortalHistoryEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CustomerPortalHistoryEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("customer_portal_history_event")
}

model CustomerPortalInvoiceView {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCustomerPortalInvoiceView", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CustomerPortalInvoiceViewCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CustomerPortalInvoiceViewUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CustomerPortalInvoiceViewDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("customer_portal_invoice_view")
}

model CustomerPortalMessage {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCustomerPortalMessage", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CustomerPortalMessageCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CustomerPortalMessageUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CustomerPortalMessageDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("customer_portal_message")
}

model CustomerPortalPaymentMethod {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCustomerPortalPaymentMethod", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CustomerPortalPaymentMethodCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CustomerPortalPaymentMethodUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CustomerPortalPaymentMethodDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("customer_portal_payment_method")
}

model CustomerPortalProjectView {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToCustomerPortalProjectView", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("CustomerPortalProjectViewCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("CustomerPortalProjectViewUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("CustomerPortalProjectViewDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("customer_portal_project_view")
}

model DashboardBookmark {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToDashboardBookmark", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("DashboardBookmarkCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("DashboardBookmarkUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("DashboardBookmarkDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("dashboard_bookmark")
}

model DashboardFolder {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToDashboardFolder", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("DashboardFolderCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("DashboardFolderUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("DashboardFolderDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("dashboard_folder")
}

model DashboardHistoryEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToDashboardHistoryEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("DashboardHistoryEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("DashboardHistoryEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("DashboardHistoryEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("dashboard_history_event")
}

model DashboardSchedule {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToDashboardSchedule", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("DashboardScheduleCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("DashboardScheduleUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("DashboardScheduleDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("dashboard_schedule")
}

model DashboardSharing {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToDashboardSharing", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("DashboardSharingCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("DashboardSharingUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("DashboardSharingDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("dashboard_sharing")
}

model DashboardTemplate {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToDashboardTemplate", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("DashboardTemplateCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("DashboardTemplateUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("DashboardTemplateDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("dashboard_template")
}

model DashboardUserView {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToDashboardUserView", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("DashboardUserViewCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("DashboardUserViewUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("DashboardUserViewDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("dashboard_user_view")
}

model DashboardWidget {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToDashboardWidget", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("DashboardWidgetCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("DashboardWidgetUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("DashboardWidgetDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("dashboard_widget")
}

model DashboardWidgetConfig {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToDashboardWidgetConfig", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("DashboardWidgetConfigCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("DashboardWidgetConfigUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("DashboardWidgetConfigDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("dashboard_widget_config")
}

model DocumentAIClassification {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToDocumentAIClassification", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("DocumentAIClassificationCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("DocumentAIClassificationUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("DocumentAIClassificationDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("document_aiclassification")
}

model DocumentAIExtraction {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToDocumentAIExtraction", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("DocumentAIExtractionCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("DocumentAIExtractionUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("DocumentAIExtractionDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("document_aiextraction")
}

model DocumentAIHistory {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToDocumentAIHistory", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("DocumentAIHistoryCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("DocumentAIHistoryUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("DocumentAIHistoryDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("document_aihistory")
}

model DocumentAIModel {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToDocumentAIModel", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("DocumentAIModelCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("DocumentAIModelUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("DocumentAIModelDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("document_aimodel")
}

model DocumentAnnotation {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToDocumentAnnotation", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("DocumentAnnotationCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("DocumentAnnotationUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("DocumentAnnotationDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("document_annotation")
}

model DocumentAttachment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToDocumentAttachment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("DocumentAttachmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("DocumentAttachmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("DocumentAttachmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("document_attachment")
}

model DocumentChunk {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToDocumentChunk", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("DocumentChunkCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("DocumentChunkUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("DocumentChunkDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("document_chunk")
}

model DocumentComment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToDocumentComment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("DocumentCommentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("DocumentCommentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("DocumentCommentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("document_comment")
}

model DocumentEmbedding {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToDocumentEmbedding", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("DocumentEmbeddingCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("DocumentEmbeddingUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("DocumentEmbeddingDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("document_embedding")
}

model DocumentFolder {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToDocumentFolder", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("DocumentFolderCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("DocumentFolderUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("DocumentFolderDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("document_folder")
}

model DocumentHistoryEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToDocumentHistoryEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("DocumentHistoryEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("DocumentHistoryEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("DocumentHistoryEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("document_history_event")
}

model DocumentIndex {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToDocumentIndex", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("DocumentIndexCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("DocumentIndexUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("DocumentIndexDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("document_index")
}

model DocumentOCRResult {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToDocumentOCRResult", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("DocumentOCRResultCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("DocumentOCRResultUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("DocumentOCRResultDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("document_ocrresult")
}

model DocumentPermission {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToDocumentPermission", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("DocumentPermissionCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("DocumentPermissionUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("DocumentPermissionDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("document_permission")
}

model DocumentRevision {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToDocumentRevision", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("DocumentRevisionCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("DocumentRevisionUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("DocumentRevisionDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("document_revision")
}

model DocumentShareLink {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToDocumentShareLink", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("DocumentShareLinkCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("DocumentShareLinkUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("DocumentShareLinkDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("document_share_link")
}

model DocumentTag {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToDocumentTag", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("DocumentTagCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("DocumentTagUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("DocumentTagDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("document_tag")
}

model DocumentTrainingSample {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToDocumentTrainingSample", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("DocumentTrainingSampleCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("DocumentTrainingSampleUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("DocumentTrainingSampleDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("document_training_sample")
}

model DocumentVersion {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToDocumentVersion", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("DocumentVersionCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("DocumentVersionUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("DocumentVersionDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("document_version")
}

model ESignatureAttachment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToESignatureAttachment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ESignatureAttachmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ESignatureAttachmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ESignatureAttachmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("esignature_attachment")
}

model ESignatureAuditTrail {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToESignatureAuditTrail", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ESignatureAuditTrailCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ESignatureAuditTrailUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ESignatureAuditTrailDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("esignature_audit_trail")
}

model ESignatureDocument {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToESignatureDocument", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ESignatureDocumentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ESignatureDocumentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ESignatureDocumentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("esignature_document")
}

model ESignatureField {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToESignatureField", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ESignatureFieldCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ESignatureFieldUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ESignatureFieldDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("esignature_field")
}

model ESignatureHistoryEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToESignatureHistoryEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ESignatureHistoryEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ESignatureHistoryEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ESignatureHistoryEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("esignature_history_event")
}

model ESignatureNotification {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToESignatureNotification", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ESignatureNotificationCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ESignatureNotificationUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ESignatureNotificationDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("esignature_notification")
}

model ESignatureRecipient {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToESignatureRecipient", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ESignatureRecipientCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ESignatureRecipientUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ESignatureRecipientDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("esignature_recipient")
}

model ESignatureWorkflowStep {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToESignatureWorkflowStep", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ESignatureWorkflowStepCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ESignatureWorkflowStepUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ESignatureWorkflowStepDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("esignature_workflow_step")
}

model EmailAccount {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToEmailAccount", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("EmailAccountCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("EmailAccountUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("EmailAccountDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("email_account")
}

model EmailAttachment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToEmailAttachment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("EmailAttachmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("EmailAttachmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("EmailAttachmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("email_attachment")
}

model EmailBounce {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToEmailBounce", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("EmailBounceCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("EmailBounceUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("EmailBounceDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("email_bounce")
}

model EmailHistoryEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToEmailHistoryEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("EmailHistoryEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("EmailHistoryEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("EmailHistoryEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("email_history_event")
}

model EmailRecipient {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToEmailRecipient", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("EmailRecipientCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("EmailRecipientUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("EmailRecipientDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("email_recipient")
}

model EmailSendLog {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToEmailSendLog", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("EmailSendLogCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("EmailSendLogUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("EmailSendLogDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("email_send_log")
}

model EmailTemplate {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToEmailTemplate", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("EmailTemplateCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("EmailTemplateUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("EmailTemplateDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("email_template")
}

model EmailThreadLink {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToEmailThreadLink", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("EmailThreadLinkCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("EmailThreadLinkUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("EmailThreadLinkDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("email_thread_link")
}

model Employee {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToEmployee", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("EmployeeCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("EmployeeUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("EmployeeDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("employee")
}

model EmployeeAddress {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToEmployeeAddress", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("EmployeeAddressCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("EmployeeAddressUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("EmployeeAddressDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("employee_address")
}

model EmployeeCompensation {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToEmployeeCompensation", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("EmployeeCompensationCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("EmployeeCompensationUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("EmployeeCompensationDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("employee_compensation")
}

model EmployeeContact {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToEmployeeContact", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("EmployeeContactCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("EmployeeContactUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("EmployeeContactDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("employee_contact")
}

model EmployeeDepartment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToEmployeeDepartment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("EmployeeDepartmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("EmployeeDepartmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("EmployeeDepartmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("employee_department")
}

model EmployeeDocument {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToEmployeeDocument", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("EmployeeDocumentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("EmployeeDocumentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("EmployeeDocumentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("employee_document")
}

model EmployeeHistoryEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToEmployeeHistoryEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("EmployeeHistoryEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("EmployeeHistoryEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("EmployeeHistoryEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("employee_history_event")
}

model EmployeePosition {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToEmployeePosition", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("EmployeePositionCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("EmployeePositionUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("EmployeePositionDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("employee_position")
}

model EmployeeSkill {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToEmployeeSkill", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("EmployeeSkillCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("EmployeeSkillUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("EmployeeSkillDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("employee_skill")
}

model EmployeeStatus {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToEmployeeStatus", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("EmployeeStatusCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("EmployeeStatusUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("EmployeeStatusDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("employee_status")
}

model EstimateAlternate {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToEstimateAlternate", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("EstimateAlternateCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("EstimateAlternateUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("EstimateAlternateDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("estimate_alternate")
}

model EstimateAssumption {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToEstimateAssumption", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("EstimateAssumptionCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("EstimateAssumptionUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("EstimateAssumptionDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("estimate_assumption")
}

model EstimateAttachment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToEstimateAttachment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("EstimateAttachmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("EstimateAttachmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("EstimateAttachmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("estimate_attachment")
}

model EstimateComment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToEstimateComment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("EstimateCommentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("EstimateCommentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("EstimateCommentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("estimate_comment")
}

model EstimateComparison {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToEstimateComparison", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("EstimateComparisonCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("EstimateComparisonUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("EstimateComparisonDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("estimate_comparison")
}

model EstimateDiscount {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToEstimateDiscount", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("EstimateDiscountCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("EstimateDiscountUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("EstimateDiscountDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("estimate_discount")
}

model EstimateExclusion {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToEstimateExclusion", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("EstimateExclusionCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("EstimateExclusionUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("EstimateExclusionDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("estimate_exclusion")
}

model EstimateFee {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToEstimateFee", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("EstimateFeeCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("EstimateFeeUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("EstimateFeeDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("estimate_fee")
}

model EstimateHistoryEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToEstimateHistoryEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("EstimateHistoryEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("EstimateHistoryEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("EstimateHistoryEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("estimate_history_event")
}

model EstimateLineItem {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToEstimateLineItem", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("EstimateLineItemCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("EstimateLineItemUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("EstimateLineItemDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("estimate_line_item")
}

model EstimatePublicLink {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToEstimatePublicLink", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("EstimatePublicLinkCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("EstimatePublicLinkUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("EstimatePublicLinkDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("estimate_public_link")
}

model EstimateRevision {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToEstimateRevision", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("EstimateRevisionCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("EstimateRevisionUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("EstimateRevisionDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("estimate_revision")
}

model EstimateSection {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToEstimateSection", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("EstimateSectionCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("EstimateSectionUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("EstimateSectionDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("estimate_section")
}

model EstimateTax {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToEstimateTax", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("EstimateTaxCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("EstimateTaxUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("EstimateTaxDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("estimate_tax")
}

model EstimateTerm {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToEstimateTerm", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("EstimateTermCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("EstimateTermUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("EstimateTermDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("estimate_term")
}

model ExpenseAttachment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToExpenseAttachment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ExpenseAttachmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ExpenseAttachmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ExpenseAttachmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("expense_attachment")
}

model ExpenseCategory {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToExpenseCategory", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ExpenseCategoryCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ExpenseCategoryUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ExpenseCategoryDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("expense_category")
}

model ExpenseHistoryEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToExpenseHistoryEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ExpenseHistoryEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ExpenseHistoryEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ExpenseHistoryEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("expense_history_event")
}

model ExpenseLine {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToExpenseLine", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ExpenseLineCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ExpenseLineUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ExpenseLineDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("expense_line")
}

model ExpensePayment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToExpensePayment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ExpensePaymentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ExpensePaymentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ExpensePaymentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("expense_payment")
}

model ExpensePolicy {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToExpensePolicy", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ExpensePolicyCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ExpensePolicyUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ExpensePolicyDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("expense_policy")
}

model ExpensePolicyViolation {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToExpensePolicyViolation", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ExpensePolicyViolationCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ExpensePolicyViolationUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ExpensePolicyViolationDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("expense_policy_violation")
}

model ExpenseReceipt {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToExpenseReceipt", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ExpenseReceiptCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ExpenseReceiptUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ExpenseReceiptDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("expense_receipt")
}

model ExpenseReport {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToExpenseReport", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ExpenseReportCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ExpenseReportUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ExpenseReportDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("expense_report")
}

model GLAccount {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToGLAccount", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("GLAccountCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("GLAccountUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("GLAccountDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("glaccount")
}

model GLAccountCategory {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToGLAccountCategory", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("GLAccountCategoryCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("GLAccountCategoryUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("GLAccountCategoryDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("glaccount_category")
}

model GLAccountSegment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToGLAccountSegment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("GLAccountSegmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("GLAccountSegmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("GLAccountSegmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("glaccount_segment")
}

model GLFiscalPeriod {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToGLFiscalPeriod", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("GLFiscalPeriodCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("GLFiscalPeriodUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("GLFiscalPeriodDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("glfiscal_period")
}

model GLFiscalYear {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToGLFiscalYear", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("GLFiscalYearCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("GLFiscalYearUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("GLFiscalYearDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("glfiscal_year")
}

model GLHistoryEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToGLHistoryEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("GLHistoryEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("GLHistoryEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("GLHistoryEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("glhistory_event")
}

model GLJournal {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToGLJournal", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("GLJournalCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("GLJournalUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("GLJournalDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("gljournal")
}

model GLJournalLine {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToGLJournalLine", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("GLJournalLineCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("GLJournalLineUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("GLJournalLineDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("gljournal_line")
}

model GLPostingBatch {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToGLPostingBatch", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("GLPostingBatchCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("GLPostingBatchUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("GLPostingBatchDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("glposting_batch")
}

model GLTrialBalanceSnapshot {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToGLTrialBalanceSnapshot", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("GLTrialBalanceSnapshotCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("GLTrialBalanceSnapshotUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("GLTrialBalanceSnapshotDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("gltrial_balance_snapshot")
}

model IntegrationApiKey {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToIntegrationApiKey", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("IntegrationApiKeyCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("IntegrationApiKeyUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("IntegrationApiKeyDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("integration_api_key")
}

model IntegrationAttachment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToIntegrationAttachment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("IntegrationAttachmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("IntegrationAttachmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("IntegrationAttachmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("integration_attachment")
}

model IntegrationConnection {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToIntegrationConnection", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("IntegrationConnectionCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("IntegrationConnectionUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("IntegrationConnectionDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("integration_connection")
}

model IntegrationConnectionHistory {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToIntegrationConnectionHistory", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("IntegrationConnectionHistoryCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("IntegrationConnectionHistoryUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("IntegrationConnectionHistoryDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("integration_connection_history")
}

model IntegrationError {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToIntegrationError", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("IntegrationErrorCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("IntegrationErrorUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("IntegrationErrorDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("integration_error")
}

model IntegrationEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToIntegrationEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("IntegrationEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("IntegrationEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("IntegrationEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("integration_event")
}

model IntegrationFieldTransform {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToIntegrationFieldTransform", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("IntegrationFieldTransformCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("IntegrationFieldTransformUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("IntegrationFieldTransformDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("integration_field_transform")
}

model IntegrationHistoryEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToIntegrationHistoryEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("IntegrationHistoryEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("IntegrationHistoryEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("IntegrationHistoryEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("integration_history_event")
}

model IntegrationInboundWebhook {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToIntegrationInboundWebhook", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("IntegrationInboundWebhookCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("IntegrationInboundWebhookUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("IntegrationInboundWebhookDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("integration_inbound_webhook")
}

model IntegrationMapping {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToIntegrationMapping", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("IntegrationMappingCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("IntegrationMappingUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("IntegrationMappingDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("integration_mapping")
}

model IntegrationOAuthToken {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToIntegrationOAuthToken", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("IntegrationOAuthTokenCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("IntegrationOAuthTokenUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("IntegrationOAuthTokenDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("integration_oauth_token")
}

model IntegrationProvider {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToIntegrationProvider", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("IntegrationProviderCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("IntegrationProviderUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("IntegrationProviderDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("integration_provider")
}

model IntegrationQueueItem {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToIntegrationQueueItem", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("IntegrationQueueItemCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("IntegrationQueueItemUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("IntegrationQueueItemDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("integration_queue_item")
}

model IntegrationRateLimit {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToIntegrationRateLimit", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("IntegrationRateLimitCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("IntegrationRateLimitUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("IntegrationRateLimitDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("integration_rate_limit")
}

model IntegrationRetryPolicy {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToIntegrationRetryPolicy", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("IntegrationRetryPolicyCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("IntegrationRetryPolicyUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("IntegrationRetryPolicyDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("integration_retry_policy")
}

model IntegrationSchemaVersion {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToIntegrationSchemaVersion", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("IntegrationSchemaVersionCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("IntegrationSchemaVersionUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("IntegrationSchemaVersionDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("integration_schema_version")
}

model IntegrationSyncJob {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToIntegrationSyncJob", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("IntegrationSyncJobCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("IntegrationSyncJobUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("IntegrationSyncJobDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("integration_sync_job")
}

model IntegrationSyncLog {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToIntegrationSyncLog", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("IntegrationSyncLogCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("IntegrationSyncLogUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("IntegrationSyncLogDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("integration_sync_log")
}

model IntegrationWebhook {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToIntegrationWebhook", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("IntegrationWebhookCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("IntegrationWebhookUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("IntegrationWebhookDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("integration_webhook")
}

model InventoryAdjustment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToInventoryAdjustment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("InventoryAdjustmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("InventoryAdjustmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("InventoryAdjustmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("inventory_adjustment")
}

model InventoryAttachment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToInventoryAttachment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("InventoryAttachmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("InventoryAttachmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("InventoryAttachmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("inventory_attachment")
}

model InventoryAudit {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToInventoryAudit", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("InventoryAuditCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("InventoryAuditUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("InventoryAuditDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("inventory_audit")
}

model InventoryAuditLine {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToInventoryAuditLine", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("InventoryAuditLineCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("InventoryAuditLineUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("InventoryAuditLineDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("inventory_audit_line")
}

model InventoryBin {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToInventoryBin", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("InventoryBinCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("InventoryBinUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("InventoryBinDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("inventory_bin")
}

model InventoryCategory {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToInventoryCategory", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("InventoryCategoryCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("InventoryCategoryUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("InventoryCategoryDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("inventory_category")
}

model InventoryCommitment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToInventoryCommitment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("InventoryCommitmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("InventoryCommitmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("InventoryCommitmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("inventory_commitment")
}

model InventoryControlHistory {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToInventoryControlHistory", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("InventoryControlHistoryCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("InventoryControlHistoryUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("InventoryControlHistoryDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("inventory_control_history")
}

model InventoryCount {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToInventoryCount", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("InventoryCountCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("InventoryCountUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("InventoryCountDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("inventory_count")
}

model InventoryCountLine {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToInventoryCountLine", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("InventoryCountLineCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("InventoryCountLineUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("InventoryCountLineDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("inventory_count_line")
}

model InventoryHistoryEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToInventoryHistoryEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("InventoryHistoryEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("InventoryHistoryEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("InventoryHistoryEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("inventory_history_event")
}

model InventoryItem {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToInventoryItem", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("InventoryItemCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("InventoryItemUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("InventoryItemDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("inventory_item")
}

model InventoryItemVendor {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToInventoryItemVendor", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("InventoryItemVendorCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("InventoryItemVendorUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("InventoryItemVendorDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("inventory_item_vendor")
}

model InventoryLocation {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToInventoryLocation", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("InventoryLocationCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("InventoryLocationUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("InventoryLocationDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("inventory_location")
}

model InventoryLossCause {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToInventoryLossCause", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("InventoryLossCauseCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("InventoryLossCauseUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("InventoryLossCauseDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("inventory_loss_cause")
}

model InventoryLossEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToInventoryLossEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("InventoryLossEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("InventoryLossEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("InventoryLossEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("inventory_loss_event")
}

model InventoryLossInvestigation {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToInventoryLossInvestigation", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("InventoryLossInvestigationCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("InventoryLossInvestigationUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("InventoryLossInvestigationDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("inventory_loss_investigation")
}

model InventoryReorderPoint {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToInventoryReorderPoint", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("InventoryReorderPointCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("InventoryReorderPointUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("InventoryReorderPointDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("inventory_reorder_point")
}

model InventoryReservation {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToInventoryReservation", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("InventoryReservationCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("InventoryReservationUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("InventoryReservationDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("inventory_reservation")
}

model InventoryReturn {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToInventoryReturn", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("InventoryReturnCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("InventoryReturnUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("InventoryReturnDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("inventory_return")
}

model InventoryReturnLine {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToInventoryReturnLine", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("InventoryReturnLineCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("InventoryReturnLineUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("InventoryReturnLineDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("inventory_return_line")
}

model InventorySafetyStock {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToInventorySafetyStock", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("InventorySafetyStockCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("InventorySafetyStockUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("InventorySafetyStockDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("inventory_safety_stock")
}

model InventoryStock {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToInventoryStock", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("InventoryStockCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("InventoryStockUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("InventoryStockDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("inventory_stock")
}

model InventorySupplier {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToInventorySupplier", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("InventorySupplierCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("InventorySupplierUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("InventorySupplierDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("inventory_supplier")
}

model InventoryTransaction {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToInventoryTransaction", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("InventoryTransactionCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("InventoryTransactionUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("InventoryTransactionDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("inventory_transaction")
}

model InventoryTransactionHistory {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToInventoryTransactionHistory", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("InventoryTransactionHistoryCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("InventoryTransactionHistoryUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("InventoryTransactionHistoryDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("inventory_transaction_history")
}

model InventoryTransactionLine {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToInventoryTransactionLine", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("InventoryTransactionLineCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("InventoryTransactionLineUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("InventoryTransactionLineDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("inventory_transaction_line")
}

model InventoryTransfer {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToInventoryTransfer", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("InventoryTransferCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("InventoryTransferUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("InventoryTransferDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("inventory_transfer")
}

model InventoryTransferLine {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToInventoryTransferLine", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("InventoryTransferLineCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("InventoryTransferLineUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("InventoryTransferLineDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("inventory_transfer_line")
}

model InventoryUnitOfMeasure {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToInventoryUnitOfMeasure", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("InventoryUnitOfMeasureCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("InventoryUnitOfMeasureUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("InventoryUnitOfMeasureDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("inventory_unit_of_measure")
}

model InvoiceAdjustment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToInvoiceAdjustment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("InvoiceAdjustmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("InvoiceAdjustmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("InvoiceAdjustmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("invoice_adjustment")
}

model InvoiceAttachment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToInvoiceAttachment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("InvoiceAttachmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("InvoiceAttachmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("InvoiceAttachmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("invoice_attachment")
}

model InvoiceComment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToInvoiceComment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("InvoiceCommentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("InvoiceCommentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("InvoiceCommentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("invoice_comment")
}

model InvoiceCredit {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToInvoiceCredit", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("InvoiceCreditCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("InvoiceCreditUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("InvoiceCreditDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("invoice_credit")
}

model InvoiceDebit {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToInvoiceDebit", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("InvoiceDebitCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("InvoiceDebitUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("InvoiceDebitDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("invoice_debit")
}

model InvoiceDiscount {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToInvoiceDiscount", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("InvoiceDiscountCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("InvoiceDiscountUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("InvoiceDiscountDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("invoice_discount")
}

model InvoiceFee {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToInvoiceFee", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("InvoiceFeeCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("InvoiceFeeUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("InvoiceFeeDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("invoice_fee")
}

model InvoiceHistory {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToInvoiceHistory", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("InvoiceHistoryCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("InvoiceHistoryUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("InvoiceHistoryDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("invoice_history")
}

model InvoiceLineItem {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToInvoiceLineItem", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("InvoiceLineItemCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("InvoiceLineItemUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("InvoiceLineItemDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("invoice_line_item")
}

model InvoiceMilestone {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToInvoiceMilestone", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("InvoiceMilestoneCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("InvoiceMilestoneUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("InvoiceMilestoneDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("invoice_milestone")
}

model InvoicePaymentApplication {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToInvoicePaymentApplication", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("InvoicePaymentApplicationCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("InvoicePaymentApplicationUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("InvoicePaymentApplicationDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("invoice_payment_application")
}

model InvoiceProgress {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToInvoiceProgress", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("InvoiceProgressCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("InvoiceProgressUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("InvoiceProgressDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("invoice_progress")
}

model InvoicePublicLink {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToInvoicePublicLink", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("InvoicePublicLinkCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("InvoicePublicLinkUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("InvoicePublicLinkDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("invoice_public_link")
}

model InvoiceReminder {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToInvoiceReminder", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("InvoiceReminderCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("InvoiceReminderUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("InvoiceReminderDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("invoice_reminder")
}

model InvoiceRetainage {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToInvoiceRetainage", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("InvoiceRetainageCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("InvoiceRetainageUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("InvoiceRetainageDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("invoice_retainage")
}

model InvoiceRevision {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToInvoiceRevision", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("InvoiceRevisionCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("InvoiceRevisionUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("InvoiceRevisionDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("invoice_revision")
}

model InvoiceTax {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToInvoiceTax", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("InvoiceTaxCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("InvoiceTaxUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("InvoiceTaxDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("invoice_tax")
}

model JobCostBudget {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToJobCostBudget", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("JobCostBudgetCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("JobCostBudgetUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("JobCostBudgetDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("job_cost_budget")
}

model JobCostBudgetLine {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToJobCostBudgetLine", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("JobCostBudgetLineCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("JobCostBudgetLineUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("JobCostBudgetLineDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("job_cost_budget_line")
}

model JobCostForecast {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToJobCostForecast", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("JobCostForecastCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("JobCostForecastUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("JobCostForecastDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("job_cost_forecast")
}

model JobCostHistoryEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToJobCostHistoryEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("JobCostHistoryEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("JobCostHistoryEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("JobCostHistoryEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("job_cost_history_event")
}

model JobCostLedger {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToJobCostLedger", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("JobCostLedgerCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("JobCostLedgerUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("JobCostLedgerDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("job_cost_ledger")
}

model JobCostLine {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToJobCostLine", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("JobCostLineCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("JobCostLineUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("JobCostLineDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("job_cost_line")
}

model Member {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToMember", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("MemberCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("MemberUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("MemberDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("member")
}

model MemberDocument {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToMemberDocument", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("MemberDocumentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("MemberDocumentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("MemberDocumentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("member_document")
}

model MemberExternalLink {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToMemberExternalLink", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("MemberExternalLinkCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("MemberExternalLinkUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("MemberExternalLinkDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("member_external_link")
}

model MemberHistoryEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToMemberHistoryEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("MemberHistoryEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("MemberHistoryEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("MemberHistoryEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("member_history_event")
}

model MemberInvitation {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToMemberInvitation", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("MemberInvitationCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("MemberInvitationUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("MemberInvitationDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("member_invitation")
}

model MemberRole {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToMemberRole", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("MemberRoleCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("MemberRoleUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("MemberRoleDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("member_role")
}

model MemberSettings {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToMemberSettings", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("MemberSettingsCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("MemberSettingsUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("MemberSettingsDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("member_settings")
}

model Message {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToMessage", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("MessageCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("MessageUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("MessageDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("message")
}

model MessageAttachment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToMessageAttachment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("MessageAttachmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("MessageAttachmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("MessageAttachmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("message_attachment")
}

model MessageHistoryEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToMessageHistoryEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("MessageHistoryEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("MessageHistoryEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("MessageHistoryEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("message_history_event")
}

model MessageMention {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToMessageMention", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("MessageMentionCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("MessageMentionUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("MessageMentionDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("message_mention")
}

model MessageParticipant {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToMessageParticipant", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("MessageParticipantCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("MessageParticipantUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("MessageParticipantDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("message_participant")
}

model MessagePin {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToMessagePin", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("MessagePinCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("MessagePinUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("MessagePinDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("message_pin")
}

model MessageReaction {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToMessageReaction", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("MessageReactionCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("MessageReactionUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("MessageReactionDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("message_reaction")
}

model MessageReadReceipt {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToMessageReadReceipt", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("MessageReadReceiptCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("MessageReadReceiptUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("MessageReadReceiptDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("message_read_receipt")
}

model MessageVisibilityRule {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToMessageVisibilityRule", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("MessageVisibilityRuleCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("MessageVisibilityRuleUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("MessageVisibilityRuleDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("message_visibility_rule")
}

model NotificationAttachment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToNotificationAttachment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("NotificationAttachmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("NotificationAttachmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("NotificationAttachmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("notification_attachment")
}

model NotificationChannel {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToNotificationChannel", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("NotificationChannelCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("NotificationChannelUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("NotificationChannelDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("notification_channel")
}

model NotificationDelivery {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToNotificationDelivery", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("NotificationDeliveryCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("NotificationDeliveryUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("NotificationDeliveryDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("notification_delivery")
}

model NotificationDigest {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToNotificationDigest", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("NotificationDigestCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("NotificationDigestUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("NotificationDigestDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("notification_digest")
}

model NotificationHistoryEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToNotificationHistoryEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("NotificationHistoryEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("NotificationHistoryEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("NotificationHistoryEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("notification_history_event")
}

model NotificationPreference {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToNotificationPreference", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("NotificationPreferenceCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("NotificationPreferenceUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("NotificationPreferenceDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("notification_preference")
}

model NotificationQueueItem {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToNotificationQueueItem", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("NotificationQueueItemCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("NotificationQueueItemUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("NotificationQueueItemDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("notification_queue_item")
}

model NotificationRule {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToNotificationRule", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("NotificationRuleCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("NotificationRuleUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("NotificationRuleDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("notification_rule")
}

model NotificationTemplate {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToNotificationTemplate", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("NotificationTemplateCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("NotificationTemplateUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("NotificationTemplateDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("notification_template")
}

model Payment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToPayment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("PaymentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("PaymentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("PaymentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("payment")
}

model PaymentApplication {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToPaymentApplication", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("PaymentApplicationCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("PaymentApplicationUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("PaymentApplicationDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("payment_application")
}

model PaymentAttachment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToPaymentAttachment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("PaymentAttachmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("PaymentAttachmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("PaymentAttachmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("payment_attachment")
}

model PaymentDispute {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToPaymentDispute", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("PaymentDisputeCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("PaymentDisputeUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("PaymentDisputeDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("payment_dispute")
}

model PaymentGatewayTransaction {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToPaymentGatewayTransaction", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("PaymentGatewayTransactionCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("PaymentGatewayTransactionUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("PaymentGatewayTransactionDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("payment_gateway_transaction")
}

model PaymentHistoryEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToPaymentHistoryEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("PaymentHistoryEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("PaymentHistoryEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("PaymentHistoryEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("payment_history_event")
}

model PaymentMethod {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToPaymentMethod", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("PaymentMethodCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("PaymentMethodUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("PaymentMethodDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("payment_method")
}

model PaymentReconciliation {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToPaymentReconciliation", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("PaymentReconciliationCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("PaymentReconciliationUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("PaymentReconciliationDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("payment_reconciliation")
}

model PaymentRefund {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToPaymentRefund", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("PaymentRefundCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("PaymentRefundUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("PaymentRefundDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("payment_refund")
}

model PaymentUnapplied {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToPaymentUnapplied", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("PaymentUnappliedCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("PaymentUnappliedUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("PaymentUnappliedDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("payment_unapplied")
}

model PayrollBenefit {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToPayrollBenefit", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("PayrollBenefitCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("PayrollBenefitUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("PayrollBenefitDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("payroll_benefit")
}

model PayrollCalendar {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToPayrollCalendar", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("PayrollCalendarCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("PayrollCalendarUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("PayrollCalendarDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("payroll_calendar")
}

model PayrollCheck {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToPayrollCheck", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("PayrollCheckCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("PayrollCheckUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("PayrollCheckDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("payroll_check")
}

model PayrollDeduction {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToPayrollDeduction", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("PayrollDeductionCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("PayrollDeductionUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("PayrollDeductionDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("payroll_deduction")
}

model PayrollDirectDeposit {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToPayrollDirectDeposit", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("PayrollDirectDepositCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("PayrollDirectDepositUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("PayrollDirectDepositDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("payroll_direct_deposit")
}

model PayrollEarning {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToPayrollEarning", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("PayrollEarningCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("PayrollEarningUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("PayrollEarningDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("payroll_earning")
}

model PayrollGarnishment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToPayrollGarnishment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("PayrollGarnishmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("PayrollGarnishmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("PayrollGarnishmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("payroll_garnishment")
}

model PayrollHistoryEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToPayrollHistoryEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("PayrollHistoryEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("PayrollHistoryEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("PayrollHistoryEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("payroll_history_event")
}

model PayrollRun {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToPayrollRun", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("PayrollRunCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("PayrollRunUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("PayrollRunDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("payroll_run")
}

model PayrollTax {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToPayrollTax", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("PayrollTaxCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("PayrollTaxUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("PayrollTaxDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("payroll_tax")
}

model PhoneCallHistoryEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToPhoneCallHistoryEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("PhoneCallHistoryEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("PhoneCallHistoryEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("PhoneCallHistoryEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("phone_call_history_event")
}

model PhoneCallRecording {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToPhoneCallRecording", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("PhoneCallRecordingCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("PhoneCallRecordingUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("PhoneCallRecordingDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("phone_call_recording")
}

model PhoneIVRMenu {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToPhoneIVRMenu", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("PhoneIVRMenuCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("PhoneIVRMenuUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("PhoneIVRMenuDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("phone_ivrmenu")
}

model PhoneNumberPool {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToPhoneNumberPool", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("PhoneNumberPoolCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("PhoneNumberPoolUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("PhoneNumberPoolDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("phone_number_pool")
}

model PhoneQueue {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToPhoneQueue", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("PhoneQueueCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("PhoneQueueUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("PhoneQueueDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("phone_queue")
}

model ProjectAttachment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToProjectAttachment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ProjectAttachmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ProjectAttachmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ProjectAttachmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("project_attachment")
}

model ProjectBaseline {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToProjectBaseline", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ProjectBaselineCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ProjectBaselineUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ProjectBaselineDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("project_baseline")
}

model ProjectBudget {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToProjectBudget", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ProjectBudgetCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ProjectBudgetUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ProjectBudgetDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("project_budget")
}

model ProjectBudgetLineItem {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToProjectBudgetLineItem", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ProjectBudgetLineItemCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ProjectBudgetLineItemUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ProjectBudgetLineItemDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("project_budget_line_item")
}

model ProjectChecklistItem {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToProjectChecklistItem", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ProjectChecklistItemCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ProjectChecklistItemUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ProjectChecklistItemDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("project_checklist_item")
}

model ProjectCriticalPath {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToProjectCriticalPath", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ProjectCriticalPathCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ProjectCriticalPathUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ProjectCriticalPathDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("project_critical_path")
}

model ProjectDailyLog {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToProjectDailyLog", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ProjectDailyLogCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ProjectDailyLogUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ProjectDailyLogDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("project_daily_log")
}

model ProjectDailyLogEquipment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToProjectDailyLogEquipment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ProjectDailyLogEquipmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ProjectDailyLogEquipmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ProjectDailyLogEquipmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("project_daily_log_equipment")
}

model ProjectDailyLogLabor {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToProjectDailyLogLabor", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ProjectDailyLogLaborCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ProjectDailyLogLaborUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ProjectDailyLogLaborDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("project_daily_log_labor")
}

model ProjectDailyLogMaterial {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToProjectDailyLogMaterial", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ProjectDailyLogMaterialCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ProjectDailyLogMaterialUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ProjectDailyLogMaterialDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("project_daily_log_material")
}

model ProjectDailyLogPhoto {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToProjectDailyLogPhoto", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ProjectDailyLogPhotoCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ProjectDailyLogPhotoUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ProjectDailyLogPhotoDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("project_daily_log_photo")
}

model ProjectDecision {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToProjectDecision", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ProjectDecisionCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ProjectDecisionUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ProjectDecisionDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("project_decision")
}

model ProjectDocument {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToProjectDocument", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ProjectDocumentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ProjectDocumentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ProjectDocumentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("project_document")
}

model ProjectHistoryEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToProjectHistoryEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ProjectHistoryEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ProjectHistoryEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ProjectHistoryEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("project_history_event")
}

model ProjectIssue {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToProjectIssue", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ProjectIssueCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ProjectIssueUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ProjectIssueDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("project_issue")
}

model ProjectLocation {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToProjectLocation", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ProjectLocationCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ProjectLocationUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ProjectLocationDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("project_location")
}

model ProjectMilestone {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToProjectMilestone", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ProjectMilestoneCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ProjectMilestoneUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ProjectMilestoneDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("project_milestone")
}

model ProjectNote {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToProjectNote", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ProjectNoteCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ProjectNoteUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ProjectNoteDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("project_note")
}

model ProjectPhase {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToProjectPhase", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ProjectPhaseCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ProjectPhaseUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ProjectPhaseDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("project_phase")
}

model ProjectProgress {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToProjectProgress", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ProjectProgressCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ProjectProgressUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ProjectProgressDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("project_progress")
}

model ProjectRisk {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToProjectRisk", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ProjectRiskCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ProjectRiskUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ProjectRiskDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("project_risk")
}

model ProjectSchedule {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToProjectSchedule", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ProjectScheduleCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ProjectScheduleUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ProjectScheduleDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("project_schedule")
}

model ProjectScheduleItem {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToProjectScheduleItem", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ProjectScheduleItemCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ProjectScheduleItemUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ProjectScheduleItemDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("project_schedule_item")
}

model ProjectTask {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToProjectTask", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ProjectTaskCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ProjectTaskUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ProjectTaskDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("project_task")
}

model ProjectTaskAssignment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToProjectTaskAssignment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ProjectTaskAssignmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ProjectTaskAssignmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ProjectTaskAssignmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("project_task_assignment")
}

model ProjectTaskAttachment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToProjectTaskAttachment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ProjectTaskAttachmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ProjectTaskAttachmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ProjectTaskAttachmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("project_task_attachment")
}

model ProjectTaskComment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToProjectTaskComment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ProjectTaskCommentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ProjectTaskCommentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ProjectTaskCommentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("project_task_comment")
}

model ProjectTaskDependency {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToProjectTaskDependency", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ProjectTaskDependencyCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ProjectTaskDependencyUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ProjectTaskDependencyDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("project_task_dependency")
}

model ProjectTeamMember {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToProjectTeamMember", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ProjectTeamMemberCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ProjectTeamMemberUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ProjectTeamMemberDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("project_team_member")
}

model PurchaseOrder {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToPurchaseOrder", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("PurchaseOrderCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("PurchaseOrderUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("PurchaseOrderDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("purchase_order")
}

model PurchaseOrderAttachment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToPurchaseOrderAttachment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("PurchaseOrderAttachmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("PurchaseOrderAttachmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("PurchaseOrderAttachmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("purchase_order_attachment")
}

model PurchaseOrderHistoryEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToPurchaseOrderHistoryEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("PurchaseOrderHistoryEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("PurchaseOrderHistoryEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("PurchaseOrderHistoryEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("purchase_order_history_event")
}

model PurchaseOrderLineItem {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToPurchaseOrderLineItem", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("PurchaseOrderLineItemCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("PurchaseOrderLineItemUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("PurchaseOrderLineItemDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("purchase_order_line_item")
}

model PurchaseOrderReceipt {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToPurchaseOrderReceipt", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("PurchaseOrderReceiptCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("PurchaseOrderReceiptUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("PurchaseOrderReceiptDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("purchase_order_receipt")
}

model PurchaseOrderReceiptItem {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToPurchaseOrderReceiptItem", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("PurchaseOrderReceiptItemCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("PurchaseOrderReceiptItemUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("PurchaseOrderReceiptItemDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("purchase_order_receipt_item")
}

model PurchaseOrderReturn {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToPurchaseOrderReturn", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("PurchaseOrderReturnCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("PurchaseOrderReturnUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("PurchaseOrderReturnDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("purchase_order_return")
}

model PurchaseRequisition {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToPurchaseRequisition", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("PurchaseRequisitionCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("PurchaseRequisitionUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("PurchaseRequisitionDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("purchase_requisition")
}

model PurchaseRequisitionItem {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToPurchaseRequisitionItem", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("PurchaseRequisitionItemCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("PurchaseRequisitionItemUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("PurchaseRequisitionItemDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("purchase_requisition_item")
}

model QualityAttachment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToQualityAttachment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("QualityAttachmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("QualityAttachmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("QualityAttachmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("quality_attachment")
}

model QualityInspection {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToQualityInspection", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("QualityInspectionCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("QualityInspectionUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("QualityInspectionDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("quality_inspection")
}

model QualityInspectionHistory {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToQualityInspectionHistory", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("QualityInspectionHistoryCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("QualityInspectionHistoryUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("QualityInspectionHistoryDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("quality_inspection_history")
}

model QualityInspectionItem {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToQualityInspectionItem", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("QualityInspectionItemCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("QualityInspectionItemUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("QualityInspectionItemDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("quality_inspection_item")
}

model QualityMaterialTest {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToQualityMaterialTest", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("QualityMaterialTestCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("QualityMaterialTestUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("QualityMaterialTestDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("quality_material_test")
}

model QualityMaterialTestResult {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToQualityMaterialTestResult", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("QualityMaterialTestResultCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("QualityMaterialTestResultUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("QualityMaterialTestResultDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("quality_material_test_result")
}

model QualityNonConformance {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToQualityNonConformance", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("QualityNonConformanceCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("QualityNonConformanceUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("QualityNonConformanceDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("quality_non_conformance")
}

model QualityNonConformanceAction {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToQualityNonConformanceAction", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("QualityNonConformanceActionCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("QualityNonConformanceActionUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("QualityNonConformanceActionDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("quality_non_conformance_action")
}

model QualityPunchListItem {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToQualityPunchListItem", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("QualityPunchListItemCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("QualityPunchListItemUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("QualityPunchListItemDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("quality_punch_list_item")
}

model QualityStandard {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToQualityStandard", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("QualityStandardCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("QualityStandardUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("QualityStandardDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("quality_standard")
}

model RFIAttachment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToRFIAttachment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("RFIAttachmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("RFIAttachmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("RFIAttachmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("rfiattachment")
}

model RFICategory {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToRFICategory", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("RFICategoryCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("RFICategoryUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("RFICategoryDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("rficategory")
}

model RFIComment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToRFIComment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("RFICommentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("RFICommentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("RFICommentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("rficomment")
}

model RFIHistoryEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToRFIHistoryEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("RFIHistoryEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("RFIHistoryEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("RFIHistoryEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("rfihistory_event")
}

model RFIImpact {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToRFIImpact", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("RFIImpactCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("RFIImpactUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("RFIImpactDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("rfiimpact")
}

model RFIQuestion {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToRFIQuestion", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("RFIQuestionCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("RFIQuestionUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("RFIQuestionDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("rfiquestion")
}

model RFIRecipient {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToRFIRecipient", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("RFIRecipientCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("RFIRecipientUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("RFIRecipientDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("rfirecipient")
}

model RFIResponse {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToRFIResponse", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("RFIResponseCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("RFIResponseUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("RFIResponseDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("rfiresponse")
}

model RFIStatus {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToRFIStatus", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("RFIStatusCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("RFIStatusUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("RFIStatusDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("rfistatus")
}

model ReceivableAgingSnapshot {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToReceivableAgingSnapshot", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ReceivableAgingSnapshotCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ReceivableAgingSnapshotUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ReceivableAgingSnapshotDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("receivable_aging_snapshot")
}

model ReceivableLedger {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToReceivableLedger", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ReceivableLedgerCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ReceivableLedgerUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ReceivableLedgerDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("receivable_ledger")
}

model ReceivablePaymentApplication {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToReceivablePaymentApplication", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ReceivablePaymentApplicationCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ReceivablePaymentApplicationUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ReceivablePaymentApplicationDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("receivable_payment_application")
}

model Role {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToRole", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("RoleCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("RoleUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("RoleDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("role")
}

model RolePermission {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToRolePermission", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("RolePermissionCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("RolePermissionUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("RolePermissionDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("role_permission")
}

model RoomModel {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToRoomModel", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("RoomModelCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("RoomModelUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("RoomModelDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("room_model")
}

model RoomModelAttachment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToRoomModelAttachment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("RoomModelAttachmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("RoomModelAttachmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("RoomModelAttachmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("room_model_attachment")
}

model RoomModelCostMapping {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToRoomModelCostMapping", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("RoomModelCostMappingCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("RoomModelCostMappingUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("RoomModelCostMappingDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("room_model_cost_mapping")
}

model RoomModelHistoryEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToRoomModelHistoryEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("RoomModelHistoryEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("RoomModelHistoryEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("RoomModelHistoryEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("room_model_history_event")
}

model RoomModelItem {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToRoomModelItem", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("RoomModelItemCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("RoomModelItemUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("RoomModelItemDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("room_model_item")
}

model RoomModelMeasurement {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToRoomModelMeasurement", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("RoomModelMeasurementCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("RoomModelMeasurementUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("RoomModelMeasurementDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("room_model_measurement")
}

model RoomModelOpening {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToRoomModelOpening", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("RoomModelOpeningCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("RoomModelOpeningUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("RoomModelOpeningDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("room_model_opening")
}

model RoomModelSurface {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToRoomModelSurface", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("RoomModelSurfaceCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("RoomModelSurfaceUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("RoomModelSurfaceDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("room_model_surface")
}

model RoomModelTakeoff {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToRoomModelTakeoff", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("RoomModelTakeoffCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("RoomModelTakeoffUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("RoomModelTakeoffDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("room_model_takeoff")
}

model RoomModelWall {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToRoomModelWall", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("RoomModelWallCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("RoomModelWallUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("RoomModelWallDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("room_model_wall")
}

model RoomScanCalibration {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToRoomScanCalibration", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("RoomScanCalibrationCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("RoomScanCalibrationUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("RoomScanCalibrationDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("room_scan_calibration")
}

model RoomScanFrame {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToRoomScanFrame", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("RoomScanFrameCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("RoomScanFrameUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("RoomScanFrameDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("room_scan_frame")
}

model RoomScanHistoryEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToRoomScanHistoryEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("RoomScanHistoryEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("RoomScanHistoryEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("RoomScanHistoryEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("room_scan_history_event")
}

model RoomScanMesh {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToRoomScanMesh", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("RoomScanMeshCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("RoomScanMeshUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("RoomScanMeshDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("room_scan_mesh")
}

model RoomScanMetadata {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToRoomScanMetadata", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("RoomScanMetadataCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("RoomScanMetadataUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("RoomScanMetadataDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("room_scan_metadata")
}

model RoomScanOutput {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToRoomScanOutput", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("RoomScanOutputCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("RoomScanOutputUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("RoomScanOutputDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("room_scan_output")
}

model RoomScanPointCloud {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToRoomScanPointCloud", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("RoomScanPointCloudCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("RoomScanPointCloudUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("RoomScanPointCloudDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("room_scan_point_cloud")
}

model RoomScanProcessing {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToRoomScanProcessing", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("RoomScanProcessingCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("RoomScanProcessingUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("RoomScanProcessingDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("room_scan_processing")
}

model RoomScanSemanticLabel {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToRoomScanSemanticLabel", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("RoomScanSemanticLabelCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("RoomScanSemanticLabelUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("RoomScanSemanticLabelDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("room_scan_semantic_label")
}

model RoomScanSession {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToRoomScanSession", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("RoomScanSessionCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("RoomScanSessionUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("RoomScanSessionDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("room_scan_session")
}

model SMSAttachment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToSMSAttachment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("SMSAttachmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("SMSAttachmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("SMSAttachmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("smsattachment")
}

model SMSHistoryEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToSMSHistoryEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("SMSHistoryEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("SMSHistoryEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("SMSHistoryEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("smshistory_event")
}

model SafetyHazard {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToSafetyHazard", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("SafetyHazardCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("SafetyHazardUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("SafetyHazardDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("safety_hazard")
}

model SafetyIncident {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToSafetyIncident", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("SafetyIncidentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("SafetyIncidentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("SafetyIncidentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("safety_incident")
}

model SafetyIncidentCorrectiveAction {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToSafetyIncidentCorrectiveAction", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("SafetyIncidentCorrectiveActionCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("SafetyIncidentCorrectiveActionUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("SafetyIncidentCorrectiveActionDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("safety_incident_corrective_action")
}

model SafetyIncidentHistoryEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToSafetyIncidentHistoryEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("SafetyIncidentHistoryEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("SafetyIncidentHistoryEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("SafetyIncidentHistoryEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("safety_incident_history_event")
}

model SafetyIncidentInvestigation {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToSafetyIncidentInvestigation", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("SafetyIncidentInvestigationCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("SafetyIncidentInvestigationUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("SafetyIncidentInvestigationDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("safety_incident_investigation")
}

model SafetyIncidentPerson {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToSafetyIncidentPerson", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("SafetyIncidentPersonCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("SafetyIncidentPersonUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("SafetyIncidentPersonDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("safety_incident_person")
}

model SafetyInspection {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToSafetyInspection", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("SafetyInspectionCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("SafetyInspectionUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("SafetyInspectionDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("safety_inspection")
}

model SafetyInspectionItem {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToSafetyInspectionItem", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("SafetyInspectionItemCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("SafetyInspectionItemUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("SafetyInspectionItemDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("safety_inspection_item")
}

model SafetyTrainingRecord {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToSafetyTrainingRecord", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("SafetyTrainingRecordCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("SafetyTrainingRecordUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("SafetyTrainingRecordDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("safety_training_record")
}

model SafetyWeatherRisk {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToSafetyWeatherRisk", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("SafetyWeatherRiskCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("SafetyWeatherRiskUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("SafetyWeatherRiskDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("safety_weather_risk")
}

model Schedule {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToSchedule", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ScheduleCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ScheduleUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ScheduleDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("schedule")
}

model ScheduleAIRecommendation {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToScheduleAIRecommendation", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ScheduleAIRecommendationCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ScheduleAIRecommendationUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ScheduleAIRecommendationDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("schedule_airecommendation")
}

model ScheduleAssignment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToScheduleAssignment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ScheduleAssignmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ScheduleAssignmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ScheduleAssignmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("schedule_assignment")
}

model ScheduleAvailability {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToScheduleAvailability", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ScheduleAvailabilityCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ScheduleAvailabilityUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ScheduleAvailabilityDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("schedule_availability")
}

model ScheduleCapacity {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToScheduleCapacity", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ScheduleCapacityCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ScheduleCapacityUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ScheduleCapacityDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("schedule_capacity")
}

model ScheduleConflictingItem {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToScheduleConflictingItem", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ScheduleConflictingItemCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ScheduleConflictingItemUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ScheduleConflictingItemDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("schedule_conflicting_item")
}

model ScheduleConstraint {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToScheduleConstraint", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ScheduleConstraintCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ScheduleConstraintUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ScheduleConstraintDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("schedule_constraint")
}

model ScheduleConstraintRule {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToScheduleConstraintRule", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ScheduleConstraintRuleCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ScheduleConstraintRuleUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ScheduleConstraintRuleDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("schedule_constraint_rule")
}

model ScheduleException {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToScheduleException", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ScheduleExceptionCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ScheduleExceptionUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ScheduleExceptionDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("schedule_exception")
}

model ScheduleForecast {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToScheduleForecast", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ScheduleForecastCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ScheduleForecastUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ScheduleForecastDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("schedule_forecast")
}

model ScheduleHistoryEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToScheduleHistoryEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ScheduleHistoryEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ScheduleHistoryEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ScheduleHistoryEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("schedule_history_event")
}

model ScheduleItem {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToScheduleItem", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ScheduleItemCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ScheduleItemUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ScheduleItemDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("schedule_item")
}

model ScheduleNote {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToScheduleNote", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ScheduleNoteCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ScheduleNoteUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ScheduleNoteDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("schedule_note")
}

model ScheduleOptimizationResult {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToScheduleOptimizationResult", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ScheduleOptimizationResultCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ScheduleOptimizationResultUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ScheduleOptimizationResultDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("schedule_optimization_result")
}

model ScheduleOptimizationRun {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToScheduleOptimizationRun", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ScheduleOptimizationRunCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ScheduleOptimizationRunUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ScheduleOptimizationRunDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("schedule_optimization_run")
}

model ScheduleResource {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToScheduleResource", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ScheduleResourceCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ScheduleResourceUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ScheduleResourceDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("schedule_resource")
}

model ScheduleShift {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToScheduleShift", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ScheduleShiftCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ScheduleShiftUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ScheduleShiftDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("schedule_shift")
}

model ScheduleTimeOff {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToScheduleTimeOff", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ScheduleTimeOffCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ScheduleTimeOffUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ScheduleTimeOffDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("schedule_time_off")
}

model ScheduleTravelTime {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToScheduleTravelTime", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ScheduleTravelTimeCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ScheduleTravelTimeUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ScheduleTravelTimeDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("schedule_travel_time")
}

model ScheduleWeatherAdjustment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToScheduleWeatherAdjustment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ScheduleWeatherAdjustmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ScheduleWeatherAdjustmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ScheduleWeatherAdjustmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("schedule_weather_adjustment")
}

model ServiceAccount {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToServiceAccount", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ServiceAccountCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ServiceAccountUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ServiceAccountDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("service_account")
}

model ServiceAccountKey {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToServiceAccountKey", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ServiceAccountKeyCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ServiceAccountKeyUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ServiceAccountKeyDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("service_account_key")
}

model ServiceContract {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToServiceContract", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ServiceContractCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ServiceContractUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ServiceContractDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("service_contract")
}

model ServiceContractCancelation {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToServiceContractCancelation", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ServiceContractCancelationCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ServiceContractCancelationUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ServiceContractCancelationDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("service_contract_cancelation")
}

model ServiceContractHistoryEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToServiceContractHistoryEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ServiceContractHistoryEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ServiceContractHistoryEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ServiceContractHistoryEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("service_contract_history_event")
}

model ServiceContractNotification {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToServiceContractNotification", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ServiceContractNotificationCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ServiceContractNotificationUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ServiceContractNotificationDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("service_contract_notification")
}

model ServiceContractPaymentMethod {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToServiceContractPaymentMethod", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ServiceContractPaymentMethodCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ServiceContractPaymentMethodUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ServiceContractPaymentMethodDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("service_contract_payment_method")
}

model ServiceContractPlan {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToServiceContractPlan", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ServiceContractPlanCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ServiceContractPlanUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ServiceContractPlanDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("service_contract_plan")
}

model ServiceContractPricing {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToServiceContractPricing", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ServiceContractPricingCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ServiceContractPricingUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ServiceContractPricingDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("service_contract_pricing")
}

model ServiceContractRenewal {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToServiceContractRenewal", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ServiceContractRenewalCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ServiceContractRenewalUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ServiceContractRenewalDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("service_contract_renewal")
}

model ServiceContractSchedule {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToServiceContractSchedule", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ServiceContractScheduleCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ServiceContractScheduleUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ServiceContractScheduleDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("service_contract_schedule")
}

model ServiceContractTask {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToServiceContractTask", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ServiceContractTaskCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ServiceContractTaskUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ServiceContractTaskDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("service_contract_task")
}

model SubmittalAttachment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToSubmittalAttachment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("SubmittalAttachmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("SubmittalAttachmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("SubmittalAttachmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("submittal_attachment")
}

model SubmittalDistribution {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToSubmittalDistribution", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("SubmittalDistributionCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("SubmittalDistributionUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("SubmittalDistributionDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("submittal_distribution")
}

model SubmittalHistoryEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToSubmittalHistoryEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("SubmittalHistoryEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("SubmittalHistoryEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("SubmittalHistoryEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("submittal_history_event")
}

model SubmittalItem {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToSubmittalItem", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("SubmittalItemCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("SubmittalItemUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("SubmittalItemDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("submittal_item")
}

model SubmittalReview {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToSubmittalReview", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("SubmittalReviewCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("SubmittalReviewUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("SubmittalReviewDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("submittal_review")
}

model SubmittalReviewer {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToSubmittalReviewer", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("SubmittalReviewerCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("SubmittalReviewerUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("SubmittalReviewerDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("submittal_reviewer")
}

model SubmittalSpecSection {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToSubmittalSpecSection", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("SubmittalSpecSectionCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("SubmittalSpecSectionUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("SubmittalSpecSectionDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("submittal_spec_section")
}

model SubmittalStatus {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToSubmittalStatus", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("SubmittalStatusCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("SubmittalStatusUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("SubmittalStatusDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("submittal_status")
}

model SubmittalWorkflowStep {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToSubmittalWorkflowStep", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("SubmittalWorkflowStepCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("SubmittalWorkflowStepUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("SubmittalWorkflowStepDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("submittal_workflow_step")
}

model Task {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToTask", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("TaskCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("TaskUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("TaskDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("task")
}

model TaskAssignment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToTaskAssignment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("TaskAssignmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("TaskAssignmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("TaskAssignmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("task_assignment")
}

model TaskAttachment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToTaskAttachment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("TaskAttachmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("TaskAttachmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("TaskAttachmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("task_attachment")
}

model TaskChecklistItem {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToTaskChecklistItem", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("TaskChecklistItemCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("TaskChecklistItemUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("TaskChecklistItemDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("task_checklist_item")
}

model TaskComment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToTaskComment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("TaskCommentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("TaskCommentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("TaskCommentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("task_comment")
}

model TaskDependency {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToTaskDependency", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("TaskDependencyCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("TaskDependencyUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("TaskDependencyDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("task_dependency")
}

model TaskHistoryEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToTaskHistoryEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("TaskHistoryEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("TaskHistoryEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("TaskHistoryEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("task_history_event")
}

model TaskLabel {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToTaskLabel", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("TaskLabelCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("TaskLabelUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("TaskLabelDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("task_label")
}

model TaskLabelAssignment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToTaskLabelAssignment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("TaskLabelAssignmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("TaskLabelAssignmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("TaskLabelAssignmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("task_label_assignment")
}

model TaskReminder {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToTaskReminder", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("TaskReminderCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("TaskReminderUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("TaskReminderDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("task_reminder")
}

model TaxCode {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToTaxCode", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("TaxCodeCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("TaxCodeUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("TaxCodeDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("tax_code")
}

model TaxExemptionCertificate {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToTaxExemptionCertificate", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("TaxExemptionCertificateCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("TaxExemptionCertificateUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("TaxExemptionCertificateDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("tax_exemption_certificate")
}

model TaxFilingAttachment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToTaxFilingAttachment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("TaxFilingAttachmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("TaxFilingAttachmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("TaxFilingAttachmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("tax_filing_attachment")
}

model TaxHistoryEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToTaxHistoryEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("TaxHistoryEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("TaxHistoryEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("TaxHistoryEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("tax_history_event")
}

model TaxJurisdiction {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToTaxJurisdiction", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("TaxJurisdictionCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("TaxJurisdictionUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("TaxJurisdictionDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("tax_jurisdiction")
}

model TaxLiability {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToTaxLiability", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("TaxLiabilityCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("TaxLiabilityUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("TaxLiabilityDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("tax_liability")
}

model TaxPayment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToTaxPayment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("TaxPaymentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("TaxPaymentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("TaxPaymentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("tax_payment")
}

model TaxRate {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToTaxRate", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("TaxRateCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("TaxRateUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("TaxRateDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("tax_rate")
}

model TaxReturn {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToTaxReturn", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("TaxReturnCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("TaxReturnUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("TaxReturnDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("tax_return")
}

model TaxRule {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToTaxRule", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("TaxRuleCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("TaxRuleUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("TaxRuleDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("tax_rule")
}

model TenantIdentityProvider {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToTenantIdentityProvider", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("TenantIdentityProviderCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("TenantIdentityProviderUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("TenantIdentityProviderDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("tenant_identity_provider")
}

model Timesheet {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToTimesheet", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("TimesheetCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("TimesheetUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("TimesheetDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("timesheet")
}

model TimesheetAdjustment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToTimesheetAdjustment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("TimesheetAdjustmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("TimesheetAdjustmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("TimesheetAdjustmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("timesheet_adjustment")
}

model TimesheetBreak {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToTimesheetBreak", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("TimesheetBreakCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("TimesheetBreakUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("TimesheetBreakDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("timesheet_break")
}

model TimesheetEntry {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToTimesheetEntry", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("TimesheetEntryCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("TimesheetEntryUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("TimesheetEntryDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("timesheet_entry")
}

model TimesheetExport {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToTimesheetExport", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("TimesheetExportCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("TimesheetExportUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("TimesheetExportDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("timesheet_export")
}

model TimesheetGeoLocation {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToTimesheetGeoLocation", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("TimesheetGeoLocationCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("TimesheetGeoLocationUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("TimesheetGeoLocationDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("timesheet_geo_location")
}

model TimesheetHistoryEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToTimesheetHistoryEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("TimesheetHistoryEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("TimesheetHistoryEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("TimesheetHistoryEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("timesheet_history_event")
}

model TimesheetOvertime {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToTimesheetOvertime", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("TimesheetOvertimeCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("TimesheetOvertimeUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("TimesheetOvertimeDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("timesheet_overtime")
}

model TimesheetSignature {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToTimesheetSignature", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("TimesheetSignatureCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("TimesheetSignatureUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("TimesheetSignatureDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("timesheet_signature")
}

model Transaction {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToTransaction", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("TransactionCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("TransactionUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("TransactionDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("transaction")
}

model TransactionAllocation {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToTransactionAllocation", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("TransactionAllocationCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("TransactionAllocationUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("TransactionAllocationDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("transaction_allocation")
}

model TransactionAttachment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToTransactionAttachment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("TransactionAttachmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("TransactionAttachmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("TransactionAttachmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("transaction_attachment")
}

model TransactionBatch {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToTransactionBatch", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("TransactionBatchCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("TransactionBatchUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("TransactionBatchDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("transaction_batch")
}

model TransactionHistoryEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToTransactionHistoryEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("TransactionHistoryEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("TransactionHistoryEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("TransactionHistoryEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("transaction_history_event")
}

model TransactionLine {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToTransactionLine", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("TransactionLineCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("TransactionLineUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("TransactionLineDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("transaction_line")
}

model TransactionReversal {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToTransactionReversal", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("TransactionReversalCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("TransactionReversalUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("TransactionReversalDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("transaction_reversal")
}

model TransactionSourceLink {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToTransactionSourceLink", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("TransactionSourceLinkCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("TransactionSourceLinkUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("TransactionSourceLinkDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("transaction_source_link")
}

model TransactionType {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToTransactionType", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("TransactionTypeCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("TransactionTypeUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("TransactionTypeDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("transaction_type")
}

model WeatherAlert {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToWeatherAlert", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("WeatherAlertCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("WeatherAlertUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("WeatherAlertDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("weather_alert")
}

model WeatherAttachment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToWeatherAttachment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("WeatherAttachmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("WeatherAttachmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("WeatherAttachmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("weather_attachment")
}

model WeatherCondition {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToWeatherCondition", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("WeatherConditionCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("WeatherConditionUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("WeatherConditionDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("weather_condition")
}

model WeatherDataSource {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToWeatherDataSource", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("WeatherDataSourceCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("WeatherDataSourceUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("WeatherDataSourceDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("weather_data_source")
}

model WeatherDelayRecommendation {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToWeatherDelayRecommendation", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("WeatherDelayRecommendationCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("WeatherDelayRecommendationUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("WeatherDelayRecommendationDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("weather_delay_recommendation")
}

model WeatherForecast {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToWeatherForecast", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("WeatherForecastCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("WeatherForecastUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("WeatherForecastDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("weather_forecast")
}

model WeatherHistoryEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToWeatherHistoryEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("WeatherHistoryEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("WeatherHistoryEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("WeatherHistoryEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("weather_history_event")
}

model WeatherImpactCondition {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToWeatherImpactCondition", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("WeatherImpactConditionCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("WeatherImpactConditionUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("WeatherImpactConditionDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("weather_impact_condition")
}

model WeatherImpactEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToWeatherImpactEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("WeatherImpactEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("WeatherImpactEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("WeatherImpactEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("weather_impact_event")
}

model WeatherImpactHistoryEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToWeatherImpactHistoryEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("WeatherImpactHistoryEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("WeatherImpactHistoryEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("WeatherImpactHistoryEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("weather_impact_history_event")
}

model WeatherImpactNotification {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToWeatherImpactNotification", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("WeatherImpactNotificationCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("WeatherImpactNotificationUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("WeatherImpactNotificationDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("weather_impact_notification")
}

model WeatherImpactRule {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToWeatherImpactRule", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("WeatherImpactRuleCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("WeatherImpactRuleUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("WeatherImpactRuleDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("weather_impact_rule")
}

model WeatherImpactTask {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToWeatherImpactTask", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("WeatherImpactTaskCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("WeatherImpactTaskUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("WeatherImpactTaskDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("weather_impact_task")
}

model WeatherMitigation {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToWeatherMitigation", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("WeatherMitigationCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("WeatherMitigationUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("WeatherMitigationDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("weather_mitigation")
}

model WeatherObservation {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToWeatherObservation", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("WeatherObservationCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("WeatherObservationUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("WeatherObservationDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("weather_observation")
}

model WeatherProjectForecast {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToWeatherProjectForecast", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("WeatherProjectForecastCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("WeatherProjectForecastUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("WeatherProjectForecastDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("weather_project_forecast")
}

model WeatherRiskAssessment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToWeatherRiskAssessment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("WeatherRiskAssessmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("WeatherRiskAssessmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("WeatherRiskAssessmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("weather_risk_assessment")
}

model WeatherSensor {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToWeatherSensor", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("WeatherSensorCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("WeatherSensorUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("WeatherSensorDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("weather_sensor")
}

model WeatherSensorReading {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToWeatherSensorReading", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("WeatherSensorReadingCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("WeatherSensorReadingUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("WeatherSensorReadingDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("weather_sensor_reading")
}

model WeatherStation {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToWeatherStation", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("WeatherStationCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("WeatherStationUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("WeatherStationDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("weather_station")
}

model WorkOrder {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToWorkOrder", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("WorkOrderCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("WorkOrderUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("WorkOrderDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("work_order")
}

model WorkOrderAssignment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToWorkOrderAssignment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("WorkOrderAssignmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("WorkOrderAssignmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("WorkOrderAssignmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("work_order_assignment")
}

model WorkOrderAttachment {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToWorkOrderAttachment", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("WorkOrderAttachmentCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("WorkOrderAttachmentUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("WorkOrderAttachmentDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("work_order_attachment")
}

model WorkOrderHistoryEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToWorkOrderHistoryEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("WorkOrderHistoryEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("WorkOrderHistoryEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("WorkOrderHistoryEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("work_order_history_event")
}

model WorkOrderInvoiceLink {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToWorkOrderInvoiceLink", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("WorkOrderInvoiceLinkCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("WorkOrderInvoiceLinkUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("WorkOrderInvoiceLinkDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("work_order_invoice_link")
}

model WorkOrderLabor {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToWorkOrderLabor", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("WorkOrderLaborCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("WorkOrderLaborUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("WorkOrderLaborDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("work_order_labor")
}

model WorkOrderMaterial {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToWorkOrderMaterial", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("WorkOrderMaterialCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("WorkOrderMaterialUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("WorkOrderMaterialDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("work_order_material")
}

model WorkOrderNote {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToWorkOrderNote", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("WorkOrderNoteCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("WorkOrderNoteUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("WorkOrderNoteDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("work_order_note")
}

model WorkOrderSignature {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToWorkOrderSignature", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("WorkOrderSignatureCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("WorkOrderSignatureUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("WorkOrderSignatureDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("work_order_signature")
}

model WorkOrderTask {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToWorkOrderTask", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("WorkOrderTaskCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("WorkOrderTaskUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("WorkOrderTaskDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("work_order_task")
}

model ZeroLossAlert {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToZeroLossAlert", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ZeroLossAlertCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ZeroLossAlertUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ZeroLossAlertDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("zero_loss_alert")
}

model ZeroLossAnalytics {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToZeroLossAnalytics", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ZeroLossAnalyticsCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ZeroLossAnalyticsUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ZeroLossAnalyticsDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("zero_loss_analytics")
}

model ZeroLossAuditTrail {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToZeroLossAuditTrail", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ZeroLossAuditTrailCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ZeroLossAuditTrailUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ZeroLossAuditTrailDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("zero_loss_audit_trail")
}

model ZeroLossCause {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToZeroLossCause", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ZeroLossCauseCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ZeroLossCauseUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ZeroLossCauseDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("zero_loss_cause")
}

model ZeroLossCorrectiveAction {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToZeroLossCorrectiveAction", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ZeroLossCorrectiveActionCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ZeroLossCorrectiveActionUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ZeroLossCorrectiveActionDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("zero_loss_corrective_action")
}

model ZeroLossEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToZeroLossEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ZeroLossEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ZeroLossEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ZeroLossEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("zero_loss_event")
}

model ZeroLossHistoryEvent {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToZeroLossHistoryEvent", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ZeroLossHistoryEventCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ZeroLossHistoryEventUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ZeroLossHistoryEventDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("zero_loss_history_event")
}

model ZeroLossInvestigation {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToZeroLossInvestigation", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ZeroLossInvestigationCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ZeroLossInvestigationUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ZeroLossInvestigationDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("zero_loss_investigation")
}

model ZeroLossItem {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToZeroLossItem", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ZeroLossItemCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ZeroLossItemUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ZeroLossItemDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("zero_loss_item")
}

model ZeroLossPreventionPlan {
    // 🆔 Identity & Tenant
  id       String @id @default(uuid(7)) @db.Uuid
  tenantId String @db.Uuid

    // 📊 Lifecycle
  status    String   @default("ACTIVE")
  version   Int      @default(1)
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @updatedAt       @db.Timestamptz(6)
  deletedAt DateTime?                 @db.Timestamptz(6)

    // 👤 Minimal audit (UUIDs; Actor relations only for CRITICAL PARENT entities)
  createdByActorId String? @db.Uuid
  updatedByActorId String? @db.Uuid
  deletedByActorId String? @db.Uuid

    // 🧠 Governance
  auditCorrelationId String? @db.Uuid
  dataClassification String   @default("INTERNAL") @db.VarChar(50)
  // retentionPolicy    RetentionPolicy?  // descomenta si tu módulo lo usa

    // 🔎 Observability (OPTIONAL: enable only if it adds real value)
  // traceId      String? @db.VarChar(64)
  // spanId       String? @db.VarChar(32)
  // authContext  String? @db.Text
    // hashToken    String? @db.VarChar(255) // add per-tenant index if you query it
  // metadata     Json?   @db.JsonB
    // recordSource String? @db.VarChar(50)  // API, IMPORT, SYSTEM
  // timezone     String? @db.VarChar(50)

    // 🔗 Required relations
  // tenant Tenant @relation("TenantToZeroLossPreventionPlan", fields: [tenantId], references: [id], onDelete: Cascade)

    // 👥 Actor relations (ENABLE ONLY for CRITICAL PARENT entities)
  // createdByActor Actor? @relation("ZeroLossPreventionPlanCreatedByActor", fields: [createdByActorId], references: [id], onDelete: SetNull)
  // updatedByActor Actor? @relation("ZeroLossPreventionPlanUpdatedByActor", fields: [updatedByActorId], references: [id], onDelete: SetNull)
  // deletedByActor Actor? @relation("ZeroLossPreventionPlanDeletedByActor", fields: [deletedByActorId], references: [id], onDelete: SetNull)

    // 🗂️ Indexes & constraints (tenant-first)
  @@unique([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, deletedAt])
  @@index([createdAt], type: Brin)
  @@index([tenantId, auditCorrelationId])
  @@index([tenantId, dataClassification])

    // Enable ONLY if you use these fields:
  // @@index([tenantId, hashToken])
  // @@index([tenantId, recordSource])
  // @@index([tenantId, timezone])
  // @@index([metadata], type: Gin)

  @@map("zero_loss_prevention_plan")
}
